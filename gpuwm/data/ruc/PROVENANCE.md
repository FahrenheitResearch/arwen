# RUC LSM setup evidence

`oracle/init.csv` was generated by compiling the unmodified WRF v4.6.1
`phys/module_sf_ruclsm.F` at commit
`d66e442fccc04111067e29274c9f9eaccc3cef28` with GNU Fortran 13.3.0.  The
single-process stubs in `tools/ruc_wrf461_oracle` replace only WRF logging and
MPI broadcast services; the called `ruclsminit` and its table loader are
unchanged.

The four columns exercise warm land, frozen land, the soil-category-14 water
branch, and fractional sea ice.  `VEGPARM.TBL`, `SOILPARM.TBL`, and
`GENPARM.TBL` are the byte-pinned WRF v4.6.1 Git blobs recorded by
`gpuwm.core.ruc_contract`.  Rebuild with:

```bash
tools/ruc_wrf461_oracle/build.sh WRF_SOURCE_ROOT BUILD_DIR
```

Canonical oracle identity:

```text
3614 bytes
f953b2cbe4990374101d427ee13b5f085cc763ef5a0b4a486f800b2a288208bf  init.csv
```

`oracle/step.csv` calls the complete unmodified `lsmruc` driver for one
60-second timestep after the verified cold start.  Its warm-rain, cold-snow,
water, and sea-ice regimes retain before/after nine-level soil state plus
surface energy/moisture fluxes, snow, runoff, and accumulation diagnostics.

```text
14042 bytes
acc2e33eb2ed79c5381a2aeff256860d649ed5b7945e124dd925771a897bd49a  step.csv
```

## Saturation lookup and sea-ice oracles

Both fixtures below were produced by the harnesses in
`tools/ruc_wrf461_oracle`, calling the unmodified WRF v4.6.1
`phys/module_sf_ruclsm.F` at commit
`d66e442fccc04111067e29274c9f9eaccc3cef28`, compiled with
`GNU Fortran (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0` at `-O0`.  Every row
carries its complete input state, so each row is reproducible on its own.
Unlike the snow fixtures, these two are reproduced by gpuwm today:
`gpuwm.core.ruc.ruc_qsn` and `gpuwm.core.ruc.ruc_sea_ice_step` match both
files bit-for-bit, and `validate_qsn_oracle.py` / `validate_sice_oracle.py`
assert exactly that inside `build.sh`.

`oracle/qsn.csv` sweeps `qsn`, the saturation lookup over the 5001-entry
`tbq` table, at 81 temperatures: nine below the first node and seven at or
above the last, where WRF clamps the index to 1 or 5000 and forces the
interpolation weight to 0 or 1; the exact 173.15 K and 423.15 K nodes; and
an interior sweep with sub-bin probes inside single 0.05 K intervals and
across the 273.15 K ice/water switch.  `r_raw` is the unclamped index
expression, written out so the branch each row took is legible from the
file.  gpuwm feeds the pinned `oracle/tbq.csv`, not the harness's in-memory
table, so the pair is validated together.

```text
3802 bytes
926e174c8e73d4103c37171c94f9a7535361759527714db97b92e792117b1219  qsn.csv
1580bf1875e5d4edcda2de21c8a5b3370037e373c980007512fcf8647defbbe3  run_qsn.F90
d1e6efff1b72bf8d0b0f1b2f25a8c3cb8ca3963874a4c67e9b0c0f8496f9fe89  validate_qsn_oracle.py
```

`oracle/sice.csv` drives `sice`, the snow-free sea-ice column: heat
diffusion through the nine ice levels plus the surface energy balance closed
by `vilka`.  The ice column properties `tice`, `rhosice`, `capice` and
`thdifice` are built from the ice temperature profile with the same Zubov
relations `lsmruc` uses and are written to the file; the surface humidity is
seeded through `qsn`, as `lsmruc` seeds it.  Its eight columns are deep cold
ice, ice at the melting point where the `min(271.4, ...)` cap binds on the
skin, thin ice with its base pinned at the ocean freezing point, a strongly
forced balance that drives the RUC evaporation branch, moist air that drives
the RUC condensation branch, the same condensing state with `myj=.true.`,
a `myj=.true.` evaporating state, and rain falling on near-melting ice.
`fltot` is identically zero because `icemelt` absorbs the whole residual,
exactly as in `snowseaice`.  `sice` itself never writes the soil water
arrays: `sfctmp` forces `soilmois=1`, `soiliqw=0`, `soilice=1`,
`smfrkeep=1`, `keepfr=0` in an identical loop after both call sites
(`:1871-1877` and `:2184-2190`), and the harness reproduces that loop after
the call so the pinned composite matches what `ruc_sea_ice_step` returns.

```text
46117 bytes
7e50fa1e46de4689fc705781a3509333c84a048fb6b066e9505ea3181f1ca69f  sice.csv
9285dd77d4623a8bf4ef3f73a7aa22897de0ef0af886e8d06d9640fa81444502  run_sice.F90
2f50323c8353a2bd07a07522aa77f3a7824be2ed76aa3ef7bc1555ce6f72590e  validate_sice_oracle.py
```

## Snow oracles

`gpuwm/core/ruc.py` and `gpuwm/core/ruc_gpu.py` port all four snow branches
`sfctmp` dispatches to: the shared snow-preparation prologue
(`ruc_snow_preparation`), `snowtemp` (`ruc_snow_temperature_step`),
`snowsoil` (`ruc_snow_soil_step`) and `snowseaice`
(`ruc_snow_sea_ice_step`), each with a CUDA twin.  Every fixture in this
section is compared against on both lanes: the `validate_*_oracle.py`
scripts `build.sh` runs fail closed on any difference, and
`tests/test_ruc.py` / `tests/test_ruc_gpu.py` assert the same parity
bitwise.

Each of the four original fixtures is paired with a second,
argument-contract fixture, because a bitwise match against a file whose
arguments are flat pins the arithmetic and not the argument list.  Mutation
studies over the finished ports measured how weak the originals were alone:
13 of 14 argument-drop mutants reproduced `snowseaice.csv`, 72 of 344
argument read sites were undetectable by `snowtemp.csv`, and five arguments
(`qcatm`, `snom`, `sat`, the entry `qsg`, the entry `ilnb`) were undetectable
by `snowsoil.csv`.  The `sfctmp` prologue has no pre-existing fixture and no
callable interface at all; it is pinned by reading the running, unmodified
module with gdb.

Except where a block below says otherwise, every fixture in this section was
produced by the harnesses in `tools/ruc_wrf461_oracle`, calling the
unmodified WRF v4.6.1 `phys/module_sf_ruclsm.F` at commit
`d66e442fccc04111067e29274c9f9eaccc3cef28`, compiled with
`GNU Fortran (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0` at `-O0`.  Every row
carries its complete input state, so each row is reproducible on its own.
The nine-level soil grid, the 5001-entry `tbq` table and a 60 s step are the
same ones the snow-free fixtures use.

One caveat applies to the snow-covered *land* lanes only.  `soilprop`,
`transf`, `soilmoist` and the freezing curve reach these fixtures bitwise,
and the CPU and CUDA lanes reach each other bitwise, only because those
routines now round a float64 transcendental once instead of calling numpy's
or the device's float32 `exp`/`pow`/`log10`.  That substitution is
PROVISIONAL and is NOT glibc -- see `_RUC_PROVISIONAL_TRANSCENDENTALS` in
`gpuwm/core/ruc.py` for the measured deviation from the live glibc 2.39.
`gpuwm/core/noahmp_libm.py` and `r_log`/`r_log10`/`r_exp`/`r_pow` in
`gpuwm/core/kernels/noahmp_leaves.cu` already carry verified glibc 2.39
transcriptions of exactly these four calls; the RUC lanes should be moved
onto them and this paragraph deleted once those are promoted out of the
Noah-MP namespace.  No fixture in this file discriminates between the two
implementations, which is why the discrimination lives in `ruc.py`'s note
and not here.

`oracle/snowtemp.csv` drives `snowtemp`, the snow energy budget and the
snow/soil heat-diffusion solve.  Its six columns are one-layer shallow fresh
snow (`rhosn` 100, 0.20 m), a two-layer aged pack (`rhosn` 350, 0.43 m), a
ripe two-layer pack that melts from the top (top melt, `rsm` retention and
the Egglston limiter), snow too thin to carry its own layer and blended into
the top soil layer, a pack over thawed ground that melts only from the
bottom (`smeltg`), and a trace pack that sublimates away entirely with
`beta < 1`.  `deltsn`, `snth`, `snwepr` and `beta` are computed by the
harness with the same arithmetic `snowsoil` performs, and are all written to
the file.  `snowtemp` declares `ilnb` `intent(out)` but reads it on the
blended path, so `ilnb_before` is pinned as part of the input state.

```text
52732 bytes
ccdc14cbe2d688627743654831fd9fc922e2453dcf86747be718d7ed76420004  snowtemp.csv
b230af3605a0bf9c74adfd542c597f4f0c97cb1585461fd3ecfba7ffd78d9b7f  run_snowtemp.F90
22294bb9c919f7191166a710165ba44c139da5a0a9ad02933d4fc0f6b02d7bd3  validate_snowtemp_oracle.py
```

`oracle/snowtemp_contract.csv` extends the `snowtemp` reference without
touching the six regimes above.  `ruc_snow_temperature_step` in
`gpuwm/core/ruc.py` and `ruc_snow_temperature_step_cuda` in
`gpuwm/core/ruc_gpu.py` reproduce all twenty-one output columns of every row
of *both* files bit for bit.

It exists because bitwise parity is only as strong as the fixture's ability
to notice the port being wrong.  A mutation study over the finished port
(`tools/ruc_wrf461_oracle/mutation_study_snowtemp.py`: replace one read of
one argument at a time, then ask whether the fixture still reproduces) found
that `snowtemp.csv` alone cannot detect 72 of 344 argument read sites being
wrong, and leaves six reachable `if` arms unexecuted.  `tranf`, for one, is
read only in the evaporation half of the melt energy budget
(`phys/module_sf_ruclsm.F:5436-5443`) and could be zeroed with no effect on
any output column.  Its eight regimes are: melt under evaporation on a dense
pack; melt where the remaining pack can evaporate entirely, so `beta` is
rewritten inside the melt block (`:5483-5491`); melt on blended snow capped
by the available snow (`:5510-5512`); unlimited two-layer bottom melt with
`hsn = snhei-deltsn` (`:5638`); bottom melt limited by `rr = snwe/delt`
(`:5663`); low-density new snow on an aged pack, which selects the `:5049`
conductivity branch through its second disjunct; unlimited melt under
condensation; and an above-283 K skin that releases both Egglston guards
(`:5499` and `:5658`), which is the only way to combine an unlimited melt
with a Koren density update.  Produced by the same harness directory, the
same pinned `phys/module_sf_ruclsm.F` at
`d66e442fccc04111067e29274c9f9eaccc3cef28`, and the same
`GNU Fortran (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0` at `-O0` as every
fixture above, via `build_snowtemp_contract.sh` rather than `build.sh` so it
regenerates without rebuilding any already-pinned CSV.

With both files the study leaves 31 arithmetic survivors, every one of them
a WRF dead store (`:5076`, `:5159-5160`, `:5164-5165`, `:5172`,
`:5180-5181`, `:5191-5194`, `:5207`), unreachable on the supported domain
(`h` is fixed at 1 at `:5209` so the `:5315-5328` retry cannot be entered,
and `snhei == 0` is rejected because `LSMRUC` gates the snow branch on
`snhei > 0` at `:1600` and `:3580` is the module's only `call snowtemp`,
so no reachable column arrives with it), or semantically equivalent (`:5339`
clamps a value already known to exceed the clamp; the `:5436` loop bound
sums `tranf` entries that `transf` leaves zero below the root zone).

`snowtemp` calls no transcendental.  Relocation-resolved disassembly of the
pinned `-O0` object shows the only calls it emits are `qsn`, `vilka`,
`wrf_at_debug_level`, one libgcc `__powisf2` (integer exponent, exact
repeated multiplication) and the Fortran I/O runtime, so no `exp`, `log`,
`pow` or `sqrt` enters either half of the port and the glibc-versus-CUDA
libm question does not arise here.  Off fixture, 4,813 perturbed columns
agree between CPU and CUDA on all twenty-one columns at `max_ulp 0`.

```text
70415 bytes
ce1c8d871e949b6057bd0b5bb8ff76c12abfed7e1439df8e7c7cdcb599e299f9  snowtemp_contract.csv
836295b3ff7c610bab7cbd2a93404d02a0c6c7f4a90cde38db657dc7ec4cc187  run_snowtemp_contract.F90
a12bc1ba855f83105e92e0c9bea2ffd66b8229c028a53836da0d4d1d96f29fbf  validate_snowtemp_contract_oracle.py
90e64e4132ab6bb171a003dd05232bffc4e76df6fc8f11c869710452f8e0c703  build_snowtemp_contract.sh
32da6ce770988428d9a8c85f19c01e2ff4976f7a696dd8c2485e7b946b303b62  mutation_study_snowtemp.py
```

`oracle/snowsoil.csv` drives `snowsoil`, the complete snow-covered land
column: the freezing-curve partition, `soilprop`, `transf`, `snowtemp`,
`soilmoist` and the surface flux diagnostics.  Soil and vegetation
parameters come from the pinned WRF tables through the unmodified
`ruclsm_soilvegparm`.  Its four columns are fresh one-layer snow over frozen
forest soil, a two-layer aged pack over frozen grassland, a melting pack
over thawed soil that also melts from below and feeds runoff and `acsnom`,
and thin blended snow over frozen cropland entered with `keepfr = 1` and an
`smfrkeep` low enough that the "ice cannot increase" cap binds.  Note that
`snowsoil` rebuilds `soilice`/`soiliqw` after `snowtemp` but before
`soilmoist`, and `soilmoist` never writes `soiliqw` back: the returned
partition satisfies `soilmois_before = soiliqw + 0.9*soilice`, not the
returned total.  The surface energy residual `fltot` stays within
0.04-17.4 W/m^2.

```text
48618 bytes
82191a381cd923d6c015a8f44ffef9262c4380b7440d1f8b5944133a899f0b78  snowsoil.csv
8cf4e08731bda95688f6d93349e9e4fcac5683d29bda77afb20ca9dd513f1ba2  run_snowsoil.F90
50b3e315290198b636cfcd9c2dd2f6590cc4d2182a84081e60b19cc39ec0a8fc  validate_snowsoil_oracle.py
```

`oracle/snowsoil_contract.csv` adds the three regimes `oracle/snowsoil.csv`
cannot use to detect an argument being dropped: `qcatm`, `snom`, `sat`, the
entry `qsg` and the entry `ilnb` are each moved off the value or the branch
the four-column file holds them at.  Its cases are
`partial_cover_two_layer`, `sublimating_patch` and `blended_retained_ilnb`;
the last enters with `ilnb = 3` on a blended column, which `snowtemp` never
assigns and therefore retains, so the incoming value is live.
`validate_snowsoil_oracle.py --contract` replays it and fails closed on any
nonzero ULP, exactly as it does for the four-regime file.  Produced by
`build_snowsoil_contract.sh`, which reuses the `module_sf_ruclsm.o` an
earlier `build.sh` produced, from the same pinned module commit and the same
compiler as every fixture above.

```text
36571 bytes
53ff7e5e7a9823b312ff7926fd863647936f6a0727d40a7d5cbac820e7a5da76  snowsoil_contract.csv
b6e9c6dda2a4bf13297682f509f52c666592e6f2c8dd5558d247123310bfae66  run_snowsoil_contract.F90
0bd583196735f7488c2117ee914d6d466bb15accf5de3aa121c7c347b0a7ad5d  build_snowsoil_contract.sh
```


`oracle/snowseaice.csv` drives `snowseaice`, the snow-on-sea-ice energy
budget and heat diffusion.  The ice column properties `tice`, `rhosice`,
`capice` and `thdifice` are built from the ice temperature profile with the
same Zubov relations `lsmruc` uses and are written to the file.  Its five
columns are blended thin snow on ice, a one-layer pack, a two-layer deep
pack, a melting pack that survives the step, and a trace pack that
sublimates away and restores the bare sea-ice albedo (0.55), roughness
(0.011) and emissivity (0.98).  Every returned ice temperature respects the
271.4 K cap and `fltot` is identically zero because `icemelt` absorbs the
residual.

```text
43087 bytes
d4be390aff9508b4b4f4519656f794129b6f40b2941212398e30ec540cb62d77  snowseaice.csv
841ff242947efd9e93ba956999a9e60703db6105be492a9991d7e3ef799f4a75  run_snowseaice.F90
7fd185da8891e73b0da7febee9757d4958790cbc8a117d1bdd84004306ed1f94  validate_snowseaice_oracle.py
```

`oracle/sfctmp_prep.csv` pins the `sfctmp` snow-preparation block,
`phys/module_sf_ruclsm.F:1400-1766`.  That block is not a subroutine: it is
straight-line code at the top of `sfctmp`, and every result is a local or an
inout dummy that the dispatch below it (`:1767-2193`) overwrites before
`sfctmp` returns, so no argument list can observe it and the physics module
may not be edited.  `run_sfctmp_prep.F90` therefore only supplies forcing and
calls the unmodified `sfctmp`; the values are read out of the running module
by gdb (`sfctmp_prep.gdb`), which breaks at `:1418` for the entry state and at
`:1767` or `:2120` -- whichever the call reaches -- for the result.  Both
land-use datasets are exercised in one run, MODI-RUC (`URBAN=13`,
`isice=15`) for the first fourteen cases and USGS-RUC (`URBAN=1`,
`isice=24`) for the last three.

Its seventeen columns are chosen to kill argument mutants, not only to cover
regimes: rain into a nearly full canopy that spills drip, a bare point that
takes the `vegfrac<=0.01` branch, a deep low-density pack whose Koren
compaction runs on a double-length step, a thin pack that turns on the mosaic
and blends roughness on the first branch, an aged dense pack that takes the
`:1496` goto-777 shortcut and blends roughness on the middle branch, new snow
plus rain whose drip is split by the mosaic, a fresh-snow event that latches
`keep_snow_albedo` and lifts `albsn` to 0.7, a graupel fall that saturates
`rhosnfall` at 500 so `keep_snow_albedo` stays clear, an urban point clamped
to 0.75, snowfall onto deep cold sea ice, partly covered melting sea ice,
thin snow on melting sea ice at the `albsn-0.1` floor, bare sea ice, snow
water carrying a reported depth of exactly zero, and three USGS-RUC points
including one whose mean snow temperature is above freezing so `min(0.,tsnav)`
has to clamp it.  `delt`, `c1sn`, `c2sn`, `sat`, `isice`, the incoming
`rhosnfall`/`emiss`/`alb`/`iland` and the land-use dataset all take at least
two distinct values, so a port that stopped reading any of them could not
reproduce the file -- `validate_sfctmp_prep_oracle.py` proves that by
hard-coding each argument to each of its observed values in turn and requiring
every such mutant to fail.

`keep_snow_albedo` and `snowfrac2` are written as `nan` on the rows where
`snhei<=0.`, because WRF only assigns them inside the `snhei>0.` branch and
nothing reads them otherwise.  `isncovr_opt` is
`integer, parameter :: isncovr_opt=2` at `module_sf_ruclsm.F:78`, so options 1
and 3 cannot appear in any pinned fixture.

```text
77585 bytes
575eb5cb13367fabb5339e721b7c030a336d6ebb87865ba20d49d0ec02b61912  sfctmp_prep.csv
38a1ed089a607b4f118bd8d34d1469407ed92579a8bf7b0a3bf45f7d0a1db32c  sfctmp_prep_inputs.csv
9974f9b187839c270f2e9b39c20a52855fce7801aab8502d483576bdfd96a20c  run_sfctmp_prep.F90
7e4fc9afa22fae219b90946e2296f03062f46a19d86c5bb322dcaa07d06c09bd  sfctmp_prep.gdb
bdfa0053590cffab8551f337d9b4b3221673cfcfd952d45cff1d350292dc04aa  validate_sfctmp_prep_oracle.py
```

`oracle/sfctmp_prep_inputs.csv` is not an oracle.  It is the driver's own
record of the forcing it passed, so the validator can confirm that gdb read
the intended variables out of the intended frame; all 34 shared columns agree
bitwise.

`oracle/snowseaice_contract.csv` is a second unmodified-module `snowseaice`
build that closes an argument-contract gap in `oracle/snowseaice.csv`.  That
file holds `myj = .false.`, `snowfrac = 1.`, `meltfactor = 1.`, `rainf = 1.`,
`ilnb = 1`, `snwe > 0` and `xlv = 2.5e6` on all forty-five rows.  Every one of
those is either a multiplicative identity or a branch the file never enters,
so the file pins the arithmetic of its five regimes without pinning the
argument list: a mutation study over the gpuwm port found that thirteen of
fourteen argument-dropping mutants reproduced `snowseaice.csv` bit for bit.
The contract file's ten columns give each argument a value or a branch where
omitting it changes an output -- `myj` on both its condensing and evaporating
sides, `snowfrac`/`meltfactor`/`rainf` together off unity, a blended pack
entered with `ilnb = 2` (`lsmruc` leaves `ilnb` undefined at
`module_sf_ruclsm.F:1385`, so the incoming value is live), `snwe = 0` on entry
so the `:4157` sublimated-pack coefficients and the `:4480` `snflx` form that
leaves `s` untouched both run, a depth inside the `:3951` `deltsn`-halving
band, a pack under 1 cm so `:4346` zeroes `rsm`, the melt block's evaporating
side, a non-default `xlv`, and a `snowh`/`snow` state drift (`lsmruc` only
re-syncs `snhei` to `snwe*1.e3/rhosn` at `:1596` under `newsn > 0`, so the
incoming `snhei` read by `:3950-3951` is independent).  After adding it,
twelve of those fourteen mutants are caught; the two survivors -- the incoming
`tsnav` and the `:4185` `bb = bb - snoh/tdenom` -- are unreachable rather than
untested.  `tsnav` is read only at `:4409-4417`, gated on the rebuilt `snhei`
being positive, and the `:4517` restore overwrites it unconditionally when it
is not; `snoh` is exactly zero on the only pass WRF makes because the `:4374`
second `vilka` iteration is commented out upstream.  The geometry, the Zubov
ice-column relations, the `tbq` construction and the CSV header are identical
to `run_snowseaice.F90`, so one reader serves both files.

```text
85283 bytes
b3605132f784d1ac6e694bdc6aaff6dfc1f843c838be96fe3b5289489505e6e7  snowseaice_contract.csv
70835b88cf9b91a79c22447095eb00570c933ddd67471e207ef841522db786dd  run_snowseaice_contract.F90
eaf3f2f77a92f8f9a67816357324cf03c6927822e594df4a75835453f55963ce  validate_snowseaice_contract_oracle.py
```

`oracle/sfctmp.csv` pins WRF `sfctmp` end to end -- the snow-preparation
prologue AND the dispatch plus mosaic recombination at
`phys/module_sf_ruclsm.F:1767-2196`.  The prologue needed gdb because its
results are locals; the dispatch does not, because it is the tail of the
subroutine and the returned arguments ARE its outputs.  `run_sfctmp.F90`
therefore just supplies forcing and calls the unmodified `sfctmp`, recording
every `intent(inout)`/`intent(out)` argument on both sides of the call: 160
columns, 33 regimes x 9 levels.  Cases 1-17 are the preparation harness's
forcing spliced in verbatim, so both fixtures drive `sfctmp` from
byte-identical state and any residue here localises to the dispatch.

The 33 regimes bind every arm: mosaic land (7 columns) and mosaic sea ice (3)
so each recombination has two independent snow fractions and cannot be
reproduced by returning an endpoint; non-mosaic snow on land (14) and on sea
ice (7); snow-free land (7) and snow-free sea ice (2), split across the
`if(alb.ne.albice)` rescale at `:2158` and the leg that skips it; the `:2077`
melt-out reset on the mosaic path, the non-mosaic path and -- case 33 -- on
sea ice; the `:2111` urban cap; both legs of the `:1997-2001` `keepfr`
selection; and the only column that drives `runoff2` off zero.  Case 29 is
the only column in the EVAPORATION regime, which is what makes `pc` and `cn`
readable at all (`transf` consults `pc` only when `lai <= 4`, `:6415-6422`,
and `(cst/sat)**cn` only matters under the 0.25 cap at `:2597`); case 32 is a
second such column on a land-use row with a different `pc`.

`validate_sfctmp_oracle.py` replays every column through
`gpuwm.core.ruc.ruc_surface_temperature_step` and then hard-codes each of the
routine's arguments to each distinct value it takes, requiring every such
mutant to fail: 741 mutants, five survivors, each argued unreachable in that
file with the WRF line that kills it (`soilice`, `soiliqw`, `qcg` and `s` are
rewritten or forced before any read on all six paths; `ilnb` is the
uninitialised local described below).

**Uninitialised `ilnb`.**  `sfctmp` declares `ilnb` at `:1385` and never
defines it.  `snowsoil` (`:1928`) and `snowseaice` (`:1956`) take it
`intent(inout)` and assign it only when the pack clears `snowtemp`'s `snth`
threshold; on the thin blended path nothing assigns it and the
`if(ilnb.gt.1)` at `:4410`/`:5716` reads stack residue.  It selects the
one-layer or two-layer `tsnav` form, so the read is observable in the output.
`run_sfctmp.F90` zeroes that stack region before every call, which is what
makes this fixture reproducible; `oracle/sfctmp_stackfill.csv` is the same
driver with a nonzero fill and is the negative control.  Over all 160 columns
of all 33 cases the pair differs in exactly five cells -- `tsnav` on cases 4,
12, 21, 26 and 28, the thin-pack mosaic columns -- and nowhere else.  Both the
validator and `tests/test_ruc.py` assert that difference set exactly.  The
port takes `ilnb` as an explicit argument so the dependence is visible rather
than accidental.

**Residue.**  3355 of 3366 cells are bitwise.  The 11 that are not are all in
two columns and were traced OUT of the dispatch with probes that call the
unmodified Fortran with exactly the arguments the dispatch hands its leaves.
For case 15 (`usgs_crop_rain_drip`) the unmodified `soil`, called with the
dispatch's own arguments, reproduces the fixture bitwise on all 13 recorded
outputs -- so the dispatch's inputs are right -- while the unmodified
`soilprop`, called with the arguments `ruc_soil_step` hands
`ruc_soil_properties`, returns `cap` bitwise but `thdif(1)` two ULP lower;
forcing that two-ULP shift makes the whole column bitwise.  The gap is
`kjpl = ke*(kasat-kdry)+kdry`, whose `ke` and `kasat` are the `log10`/`pow`
that `_RUC_PROVISIONAL_TRANSCENDENTALS` documents as float64-then-round rather
than glibc; `soiltemp`'s `vilka` then amplifies it, leaving `ts` unchanged
(the shift is below one ULP of 282 K) while `qs` moves 17 ULP, and
`dew`/`eeta`/`qfx`/`fltot` follow from `qsg`.  For case 8
(`graupel_dense_new_snow`) the same probe on the mosaic `soil` call
reproduces `tso`, `soilice`, `soiliqw`, `soilt` and `mavail` bitwise, so the
snow-free half of the recombination is exact and the 1 ULP of
`soilice`/`soiliqw` is on the `snowsoil` side; `soilprop` on that column is
one ULP low in `thdif(8)`, same class.  Fixing the transcendentals has a CUDA
half and is not part of this lane, so the residue is PINNED cell by cell with
its measured ULP in both the validator and the test: a listed cell may only
shrink, and any unlisted cell fails.

```text
545692 bytes
f7fa29f732cb82b664b13ab7a31c35ba483f95c7f04abfe2a39b854a330b0612  sfctmp.csv
a74d9373145449a5de83c07c8582c3cfc2c75e57d17a4547f4cd088ffcf849dc  sfctmp_stackfill.csv
51a1ad05d338185cf88711eb897a3b1f482c10608bb6248eb3318b6818b878b2  run_sfctmp.F90
6dbd4b562e56c9c937615e5b39258e55a014be9d5322065d7ad90a138086040b  build_sfctmp.sh
2a9f6ea2e0b4fb1a6fefe071b8377e8e770985ec15024ef41152565ca47002f9  validate_sfctmp_oracle.py
```

`oracle/sfctmp_stackfill.csv` is not an oracle in its own right -- it is the
negative control for the paragraph above, produced by
`./run_sfctmp ruc-sfctmp-stackfill.csv 12345.0` from the same binary, the same
unmodified `module_sf_ruclsm.F` (sha256
`3265f810d08dcbddfaf198371dc7f652e78e8d3a788f703a515c555a3bbb2a12`) and the
same GNU Fortran 13.3.0.

## The driver

`oracle/lsmruc.csv` pins WRF `LSMRUC` end to end,
`phys/module_sf_ruclsm.F:84-1175` -- the RUC land-surface driver that composes
everything above.  `LSMRUC` is a subroutine whose results are all returned
arguments, so unlike the `sfctmp` prologue it needs no debugger:
`run_lsmruc.F90` supplies forcing and calls the unmodified routine, recording
the complete `intent(inout)`/`intent(out)` argument set on both sides plus the
complete forcing.  122 numeric columns, 48 (run, step, column) groups x 9
levels.

Two independent runs, each integrated for two steps, because several branches
are selected by call scalars rather than by column state:

* run 1 -- `MODIFIED_IGBP_MODIS_NOAH` (MODI-RUC), `frpcpn=.true.`,
  `rdlai2d=.false.`, `dt=60`, `iswater=17`, `isice=15`
* run 2 -- `USGS` (USGS-RUC), `frpcpn=.false.`, `rdlai2d=.true.`, `dt=180`,
  `iswater=16`, `isice=24`
* step 1 is `ktau=1`, the first-step initialisation block (`:481-565`); step 2
  is `ktau=2` on the state step 1 left behind, so `sfcrunoff`, `acrunoff`,
  `sfcevp`, `snowfallac`, `acsnow` and `snom` are pinned with a nonzero
  incoming value rather than from zero.  **`udrunoff` is not** -- this line
  used to claim it was, and the corrected mutation study below measured
  otherwise; see the fixture gaps.

The twelve columns per run bind: both `frpcpn` arms and, inside the
`.not. frpcpn` arm, both sides of the 273.15 K split; water; sea ice above the
threshold and land with `0 < xice < threshold`; both `iforest` arms, so both
`meltfactor` values and both root-depth searches; all three `ktau==1` `soilt1`
arms and both the `qcg` and `qvg` reset arms; a column that drives `chklowq` to
0; snow-free columns that take the `:1082` `lemitbl` emissivity; a sea-ice
column whose `snowfrac` is scaled by `xice` at `:1105`; thin, deep, melting and
freshly-fallen snow; and urban.  `tests/test_ruc.py::test_lsmruc_binds_every_driver_arm`
asserts every one of those is non-empty.

### The mutation study, and the vacuous control it used to have

`validate_lsmruc_oracle.py` replays every group through
`gpuwm.core.ruc.ruc_land_surface_step` and then hard-codes each driver argument
to each distinct value the fixture gives it.  It used to report **218 mutants,
zero survivors**.  That number was meaningless.

The study scored a mutant as *killed* when its output was not bitwise-equal to
`lsmruc.csv`.  The unmutated port is not bitwise-equal to `lsmruc.csv` either
-- that is the 26-cell `tanhf` residue documented below -- so the **null
mutant, the port with nothing changed, was itself "killed"**, and every mutant
scored as detected whether or not the fixture could see it.  Re-run against the
correct control, the port's own output, the same 218 mutants leave **30
survivors**.  The single assertion that would have caught this, "the unmutated
port must survive", now runs first, in the validator (`_null_mutant`) and in
`tests/test_ruc.py::test_lsmruc_mutation_control_is_the_port_not_the_fixture`.

A surviving constant has two possible causes and only one of them is
unreachability: either the port never reads the argument, or it does and the
surviving constant is simply the only value the fixture ever lets that read
see.  A second study separates them and is the negative control for every
claim below -- it replays the whole fixture with one argument taken from
*another* case, leaving all others alone, at shifts 1 (crosses the column-type
boundary inside a call), 12 (crosses the timestep) and 24 (crosses the run).

**Unreachable: 6 arguments, 22 of the 30 survivors.**  No supported call can
read the entry value, and no permutation of them moves any output.

| argument | why the entry value is unreachable |
| --- | --- |
| `chklowq` | written before every read on every path: `:547` at `ktau==1`, `:843` on the water arm, the total `if`/`else` at `:1070`/`:1072` on the land arm.  Its one read, `:1076`, is inside `if(wrf_at_debug_level(...))` and only prints |
| `chs` | read at `:681`-`:682` only, both inside `if(myj)`; `ruc_land_surface_step` rejects `myj=True`, as `ruc_soil_step` and `ruc_snow_soil_step` already do |
| `precipfr` | assigned unconditionally at `:678`, above the `:828` water test, so every column overwrites it before the loop branches.  `LSMRUC` never reads it |
| `qsfc` | written before every read on every path: `:521`, `:842`, `:886`, `:1065`.  `LSMRUC` never reads it |
| `smavail` | `:830` on the water arm, `:1024` unconditionally on the land arm.  Its reads at `:1133`/`:1138`/`:1151` are the water-budget block, and `waterbudget`/`acwaterbudget` are declared at `:330-334` without `intent` -- `LSMRUC` locals that never leave the routine |
| `smmax` | `:831` on the water arm, `:1025` on the land arm.  Never read |

**Fixture gaps: 8 survivors on 8 arguments the driver really does read.**
These are *untested*, not unreachable -- the permutation control kills every
one of them -- and each is a case this fixture would need:

| survivor | the live read, and what the fixture is missing |
| --- | --- |
| `lh := 0` | `:973` assigns it on the land arm only, so a water column passes entry to exit; both water columns (cases 4 and 28) enter at 0.  Needs a water column with `lh != 0` |
| `qfx := 0` | `:972`, same water pass-through.  Needs a water column with `qfx != 0` |
| `sfcexc := 0` | `:1062` is the only write and it is on the land arm.  Needs a water column with `sfcexc != 0` |
| `z0 := 0` | `:1061` is the only write and it is on the land arm.  Needs a water column with `z0 != 0` |
| `udrunoff := 0` | `:1041` is `udrunoff = udrunoff + runoff2*dt*1000`.  `:534` zeroes it at `ktau==1`, discarding `run_lsmruc.F90:370`'s 1.0 seed, and `runoff2` is **exactly 0 in all 48 columns**, so step 2 enters at 0 too and the accumulation is only ever `0 + 0`.  Dropping the `udrunoff +` would go unnoticed.  Needs a column with `runoff2 > 0` |
| `rhosnf := -1000` | `:695` reads it into `rhosnfall`, which `SFCTMP` overwrites only when `newsn > 0`, and `:1113` writes it back.  `rainbl` and `frzfrac` are the same at both steps, so every column carrying a real density gets new snow again and has it overwritten, and every column that does pass it through entered at the `-1.e3` flag `:526` wrote.  Needs a third step, or a column whose snowfall stops |
| `znt := 1.e-4` | `SOILVEGIN:6852` keeps the caller's `znt` where `ivgtyp == iswater` -- "do not overwrite z0 over water with the table value" -- and `:6911` hands it back.  `RUCLSMINIT:7088` seeds `znt = z0tbl(ivgtyp)` for every column, water included, and `z0tbl` is `1.e-4` at MODI-RUC 17 and USGS-RUC 16, so the one value the live read can see is `1.e-4`.  In a real run the surface-layer scheme owns `znt` over water.  Needs a water-category column with `znt != z0tbl(ivgtyp)`, which is any restart |
| `iswater := 17` | selects `SOILVEGIN`'s `:6840` arm.  `iswater := 16` **is** killed, by MODI-RUC case 7 (`ivgtyp=16`), whose `lai` moves 0.75 -> 0.39 because run 1 has `rdlai2d=.false.`.  17 survives because only run 2 changes, and run 2 has `rdlai2d=.true.`, which freezes `lai` and leaves `znt` as the only channel -- closed by `RUCLSMINIT:7088` above.  Needs a USGS run with `rdlai2d=.false.` |

Both tables are exact in both directions: a survivor in neither fails the
validator, a listed entry that stops surviving fails it, an "unreachable"
argument that any permutation kills fails it, and a "fixture gap" that no
permutation kills fails it.  `tests/test_ruc.py` carries the same three
measurements.

**No port defect.**  Every one of the 30 survivors is a property of the
fixture, not of `ruc_land_surface_step`: at each of the eight live reads the
port reads the argument exactly where WRF does, including the two easy ones to
get wrong -- the water arm's `continue` leaving `lh`, `qfx`, `hfx`, `sfcexc`,
`z0`, `grdflx` and the runoff accumulators as pass-throughs, and
`ruc_surface_parameters` preserving the caller's `znt` at the water category.

**What the driver reproduces on purpose.**  `sfcevp` is accumulated twice per
column, at `:1095` and again at `:1116`, with nothing in between changing
`qfx`; the fixture shows `sfcevp` advancing by `2*qfx*dt` and the port does the
same.  `rhosnf`, `precipfr` and `snowfallac` are declared `intent(out)` at
`:344-347` but `rhosnf` is read at `:695` and `snowfallac` is read inside
`SFCTMP` at `:1641`, so both are live inputs in practice and the port takes
them as such.  `:1109-1141` (`wb`, `waterbudget`, `acwaterbudget`) and
`:906-908` are dead and are not transcribed.

**ARW additions after the historical driver fixture.**  The `EM_CORE==1`
precipitation partition (`:618-652`) and lake bypass are transcribed in the
forecast path and independently checked by the surface-forcing source probe.
The old whole-driver fixture remains an explicit `em_core=0` replay because
that is how it was compiled. SPP is still absent because its stochastic
`pattern_spp_lsm`/`field_sf` state is not ported. `mosaic_lu==1` is rejected,
so the irrigation block at `:984-1009` -- gated on the same flag that
`ruc_surface_parameters` is fail-closed on -- is unreachable. `myj=.true.` is
rejected because `ruc_soil_step` and `ruc_snow_soil_step` are, which makes
`:681-682` unreachable; that arm is transcribed but carries a comment saying
it is unverified.

**Uninitialised reads.**  `SFCTMP`'s `ilnb` (`:1385`) carries the previous
COLUMN's snow-layer count inside the driver loop; `ruc_land_surface_step`
threads that chain explicitly and returns the final value.  The driver's own
`snoh`, `snflx`, `s`, `sublim` and `evapl` automatics are read by `SFCTMP` and
are only zeroed at `ktau==1`, so from `ktau>1` WRF reads stack residue; gpuwm
defines them as zero on every call.  `oracle/lsmruc_stackfill.csv` is the same
driver with the callee stack filled with `12345.0` instead of zeroed and is the
negative control for both: over 432 rows x 122 numeric columns the pair differs
only in `tsnav` (36 cells) and in the `tsnav` carried into the next step's
entry snapshot (18 cells), on the two thin-snow columns.  So on this fixture
`ilnb` is observable and the five stale locals are not.  The validator and
`tests/test_ruc.py` both assert that difference set exactly.

**Residue, and a defect it found in `ruc_snow_preparation`.**  26 of the
fixture's cells do not reproduce bitwise.  None of them is the driver's own
arithmetic: every one is a straight pass-through of an
`ruc_surface_temperature_step` output.  25 of the 26 are a single function.
`gpuwm.core.ruc._f32_tanh` transcribes **fdlibm**'s `tanhf`, and glibc 2.39's
`tanhf` is not fdlibm's: rebuilding the fdlibm form from glibc's own `expm1f`
gives `0.760541856` where glibc `tanhf` returns `0.760541916`, at the argument
`x = 0.9974991083145142` that WRF `:1520` reaches with `tabs = 270 K`.  Over
the 80 `tanh` arguments this fixture reaches, `_f32_tanh` misses glibc on four,
by up to 3 ULP.  Substituting a measured glibc `tanhf` table for `_f32_tanh`
and replaying collapses the residue from 26 cells / 425 ULP to **one cell of
1 ULP** -- `grdflx` on `(run 1, step 2, mixed_forest_melt)`, which is the
`exp`/`pow`/`log10` class `_RUC_PROVISIONAL_TRANSCENDENTALS` already documents.

Measured over the arguments WRF `:1520-1521` reaches for air temperatures from
220 K to 310 K in 0.01 K steps -- 18,000 distinct float32 arguments, both the
`0.15` and the `0.3333` forms -- `_f32_tanh` misses glibc 2.39 `tanhf` on 231
of them, 1.28%, by up to 3 ULP.  So this is not a single unlucky argument: any
RUC fixture that lands on one of those 1.28% will show it.

That is a defect in `ruc_snow_preparation`, not in the driver.  It is pinned
cell by cell rather than fixed here because fixing it needs a verified glibc
`tanhf` transcription, its CUDA twin in `gpuwm/core/kernels/ruc.cu`, and a
re-verification of `oracle/sfctmp_prep.csv` and `oracle/sfctmp.csv`.
`oracle/lsmruc.csv` is the first RUC fixture to reach an argument where the two
`tanh` implementations disagree, which is why the snow lanes were bitwise
without it.  A listed cell may only shrink; any unlisted nonzero cell fails, in
both the validator and the test.

Produced by `tools/ruc_wrf461_oracle/build_lsmruc.sh` on top of an existing
`build.sh` tree, from the same pinned `phys/module_sf_ruclsm.F` at
`d66e442fccc04111067e29274c9f9eaccc3cef28` and the same
`GNU Fortran (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0` at `-O0` as every fixture
above.

```text
586689 bytes
c00851197b9f351cb5a14f820d0b3da6887eaae510b8c43528ce00635095b90a  lsmruc.csv
586797 bytes
26773cc2c1b79f0e12c16695f6553f33cfd0e15dbb95864903eef4cc3da83e42  lsmruc_stackfill.csv
9880121fc9a2ea5b9ccde926a20d9e0d289f2097f1baeda60b2d14f23f4e3e23  run_lsmruc.F90
491028d6769997e4f75a44906c30533caae1346c179b778ea2852625ece83fae  validate_lsmruc_oracle.py
171339ca8fb31d8661af3b60b29e30f1a84fa7285847e90852b4176d992976b6  build_lsmruc.sh
```

Both CSVs, `run_lsmruc.F90` and `build_lsmruc.sh` are byte-identical to what
`a17685b` recorded; only `validate_lsmruc_oracle.py` moved, to fix the
mutation control described above (it was
`a1f9b75f019ba96f818bdfaa0a5d451f2d0a00b1f8676dc56dba496e2a232f97`).  The
fixture itself is unchanged, which is why the eight gaps above are recorded
rather than closed: closing them needs new columns in `run_lsmruc.F90` and a
regenerated `lsmruc.csv`.

**No CUDA half.**  `gpuwm/core/ruc_gpu.py` stops at `ruc_snow_soil_step_cuda`:
`sfctmp` itself has no CUDA twin, so the driver cannot have one either.  The
driver is also the one RUC routine whose WRF answer is column-order dependent,
through the `ilnb` chain above, so a data-parallel lane will have to take
`ilnb` as an input and say so rather than reproduce the chain.

## Soil ingest -- `oracle/soil_ingest.csv`

This one is not `phys/module_sf_ruclsm.F` at all.  It is `real.exe`'s side of
RUC: `share/module_soil_pre.F`'s `init_soil_depth_3` (:1153-1194) and
`init_soil_3_real` (:1817-2158), the pair `process_soil_real` (:817-830)
selects when `sf_surface_physics == RUCLSMSCHEME`.  It exists because RUC was
admitted as a scheme with **no way to initialise it from real data**, and a
prior lane correctly declined to invent a remap with no oracle behind it.

The build in `tools/ruc_soil_ingest_wrf461_oracle` runs WRF's own `.F -> .f90`
pipeline (`configure.wrf:365-370`, including `tools/standard.exe`, which is
what rewrites `CALL wrf_error_fatal (` into `wrf_error_fatal3 ( __FILE__ ,
__LINE__ ,`) and then **checks its output against the `.f90` WRF's own build
left in `share/`** -- they are byte-identical, which is the receipt that the
preprocessing is WRF's and not a hand-patch.  `frame/module_state_description.F`
is compiled from the pinned tree rather than stubbed, because
`process_soil_real` branches on the scheme integers it defines.  Only seven
symbols are stubbed, exactly the ones `nm -u` reports on a full build's
`share/module_soil_pre.o`.

Seven experiments x 20 columns = 140 rows: a layer source at ERA5 depths with
and without `flag_sm_adj` and with and without an SST field, a layer source at
the GFS/Noah depths, a node source already on RUC's nine levels with the
adjustment both off and armed (where it must be a no-op), and one arm handing
the layers in deepest-first.

```text
108139 bytes
371a232c92adfb806751d1ba03f83e3e25c1ef7e5768be0751e30f257668ac9c  soil_ingest.csv
```

`gpuwm.ingest.ruc_soil` reproduces 118 of the 140 rows at **max_ulp 0** for
TSLB, SMOIS and TSK (`tests/test_ruc_soil_ingest.py`).  The other 22 are rows
it refuses on purpose, and the fixture is the evidence for each refusal:

* **20 rows, the deepest-first arm.**  `init_soil_3_real`'s sort (:1881-1899,
  :1913-1931) permutes `st_input(1..n)`, but a layer source lives in
  `st_input(2..n+1)` -- WRF's own reader puts it there
  (`share/module_optional_input.F:1364-1370`) so that slot 1 can hold the skin
  temperature and slot n+2 the deep temperature.  The sort therefore permutes
  the wrong slots.  All 18 land columns come back with soil temperatures of
  order -1e29 K; the two water columns look fine only because :2131-2157
  overwrites them wholesale.  metgrid emits levels shallow-to-deep so WRF
  never trips on this, but an adapter that does not is corrupted rather than
  told, so `gpuwm.ingest.ruc_soil` requires a strictly increasing level list.
* **2 rows, the all-zero-moisture land column under `flag_sm_adj`.**  :2079
  divides by `0.9*smtotr`, which is 0/0 for that column; WRF then evaluates
  `MAX(0.02, NaN)` and gfortran 13.3.0 returns 0.02.  That is an operand-order
  property of `maxss`, not of Fortran, so it is refused rather than shipped.

Two WRF behaviours ARE reproduced rather than corrected, because they are
inputs to the reference and not undefined:

* `init_soil_depth_3`'s `dzs` is **not a partition of the column**.  Its
  interface array is written at :1174-1177 and then overwritten at :1179, so
  `dzs(1)` and `dzs(2)` both cover [0, 0.005] m and `sum(dzs)` is 3.005, not
  3.0 at nine levels.  `dzs` is an input to the moisture adjustment; a
  "corrected" partition moves the shipped soil moisture by 78,081 ULP.
* The Noah source bucket at :1930-1934 weights the four source layers
  0.1/0.2/0.7/1.0.  Those are **not** Noah's own `dzs` (0.1/0.3/0.6/1.0 from
  `init_soil_depth_2`); the 2 m total agrees, the middle two do not.
