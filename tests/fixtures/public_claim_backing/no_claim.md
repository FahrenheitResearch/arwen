# Discrimination control

Prose that mentions the initial time without stating a result about it.
A guard that fired here would be a guard nobody could keep switched on.

The t=0 frame is written by the preprocessing chain before the
integration starts, and its valid time labels every later frame.

Boundary tables are staged next to it; how many sides a case carries
depends on the nesting configuration.
