# An unscoped claim over a partial receipt

The sentence names no carrier group at all, so it reads as a claim over
the whole state; the linked receipt covers one group.

The t=0 comparison reproduces the reference at the FP32/operator floor
([receipt](receipts/surface_only.json)).
