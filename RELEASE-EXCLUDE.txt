# RELEASE-EXCLUDE: internal campaign material that must NOT enter the
# public release snapshot.
#
# These paths are development-campaign records -- lead/controller
# handoffs, phase plans, machine-specific runbooks -- several of which
# bake a per-user home directory into a shipped file, contradicting the
# release builder's own "no developer-specific absolute paths" bar and
# confusing a stranger about what the product is (acceptance finding
# F13, PRODUCT-V1-ACCEPTANCE-20260729).
#
# This file is an exclusion manifest for the snapshot/release builders;
# nothing listed here is deleted from the development tree.  One path or
# glob per line, relative to the repository root; lines starting with
# '#' are comments.
#
# Exclusion is only half the job.  A manifest cannot name the file that
# grows a machine path next, so work/build_release_snapshot.py scans
# every staged text file for one and FAILs the build; see
# MACHINE_PATH_MARKERS there.  Adding a path here is the escape hatch
# for a file that is genuinely not worth scrubbing -- never the first
# resort.

# The mp_physics=28 port's own working spec and closing handoff.  The spec
# is 127 KB of work-package narrative with a developer's WRF checkout path
# in five places; the handoff is the port author's note to its successor.
# Both are development-campaign records, and both are tracked here because
# the port survived only as an unversioned directory -- committing them is
# what stops the design record from having exactly one copy again.
MP28_PORT_SPEC.md
HANDOFF.md
MP28_HANDOFF.md

# Campaign handoffs and internal project history (developer paths inside).
docs/gpuwm-controller-handoff.md
docs/lead-handoff-2026-07-26.md
docs/gpuwm-project-history.md
docs/tree_triage_2026-07-26.md
docs/assembly-execution-20260728.md

# Internal development-campaign plans/specs (machine paths inside).
docs/superpowers/**

# Internal campaign reports and machine-specific runbooks.
docs/morrison_oracle_report.md
docs/mynn_four_domain_report.md
docs/mynn_noahmp_ruc_completion_plan.md
docs/easy-physics-rtx5090-runbook.md
docs/easy-physics-integration-status.md
docs/wsm6-rtx5090-runbook.md
docs/real74-nssl2-500m-runbook.md
docs/real74-nssl2-500m-comparison-ratification-v1.md
docs/ports/rrtm-dudhia-remote-gpu-runbook.md
docs/ports/wrf-rrtm-dudhia-status.md

# Internal acceptance/handoff ledgers (the release ships its own docs).
handoffs/**
evidence/**
work/**

# Campaign ensemble harness.  Every entry point needs a privately-built
# WRF oracle (wrf.exe/real.exe at pinned SHAs), a private reference
# bundle, and the campaign's own case directory, none of which a public
# reader has or can obtain -- so the scripts are unrunnable outside the
# campaign and carry its case naming and machine layout.  Its test goes
# with it; nothing else in the tree imports tools.n5s.
tools/n5s/**
tests/test_n5s_toolchain.py

# NOT excluded, deliberately: tools/data/knob_survey_lanes.jsonl.  It
# looked like internal campaign output, but tools/build_registry.py
# reads it to regenerate gpuwm/physics_registry_v2.json and
# tests/test_build_registry.py asserts that regeneration reproduces the
# shipped file byte for byte.  Dropping it would trade a reproducibility
# proof for a hygiene win, so the one developer path inside it was
# scrubbed instead.

# A generated `sha256sum` receipt whose second column is one machine's
# absolute paths into a private reference bundle.  The hashes it pins
# are reproduced by tools/rrtmg_wrf461_oracle/sw_build.sh, which writes
# this file; nothing reads it.
tools/rrtmg_wrf461_oracle/sw_fixtures/sw-oracle-sha256sums.txt

# Externalized large asset: GitHub rejects plain blobs over 100 MiB, so
# this 243 MiB table is published as a GitHub release asset instead of
# living in the public git history.  `gpuwm fetch-tables` stages it,
# SHA-256-verified against gpuwm/core/thompson_contract.py, and the
# install scripts run that automatically; the development tree keeps
# the file tracked as the source of truth for the release asset.
gpuwm/data/thompson/tables/freezeH2O.dat

# Raw test-run transcripts must never ship at repo root: they are triage
# working evidence (the 1.4.1 battery evidence deliberately lived beside
# the repo, not in it), and a top-level failure log is the first thing a
# stranger would open (1.5.0 cut finding, 2026-08-03).
cpu_suite.log
gpu_tier.log
gpu_retry.log
gpu_failed_ids.txt

# Ensemble-sweep node drivers.  These are the shell harness that runs the
# ensemble-size ladder on the project's rented Linux nodes: they hard-code
# that layout as defaults (a sweep root, a tree root and a geography root,
# all under the node's root account), drive each other through status files
# in the same home directory, and are unrunnable anywhere else.  Spelling
# those defaults out here would put the very paths this file exists to keep
# out of the release into the release.  The Python beside them is NOT
# excluded --
# score_free_forecast.py is the scorer of record for the published FSS
# numbers, and decompose_skill.py, score_resolution.py and
# summarize_sweep.py read committed composites -- so the analysis half of
# the directory ships and only the machine-shaped driver half does not.
tools/ens_sweep/build_obs.sh
tools/ens_sweep/ceiling_probe.sh
tools/ens_sweep/chain.sh
tools/ens_sweep/chain2.sh
tools/ens_sweep/pool_limit_probe.sh
tools/ens_sweep/prepare_case.sh
tools/ens_sweep/resolution_cost_probe.sh
tools/ens_sweep/run_sweep.sh

# Grell-Freitas dual-run byte-comparison harness.  A Windows Task Scheduler
# job (task.xml) plus the PowerShell queue it launches, both written around
# one box: the queue names an absolute worktree path and a local Downloads
# directory.  Dual-run byte comparison is the project's corruption screen on
# a card without ECC, and it is worth keeping tracked, but it is a bench
# fixture for this machine rather than anything a reader can run. Nothing
# in the tree references it.
tools/gf_real74_dual/**
