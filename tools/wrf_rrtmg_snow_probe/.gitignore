# Generated by extract_and_build.sh / make_inputs.py: the .inc files are
# verbatim WRF v4.6.1 source extracts (re-created with SHA-256 verification
# of the authority), the deck and outputs are reproducible, and the binary
# is a build product.  The committed fixture lives in
# tests/data/wrf_rrtmg_snow_discount_fixture.csv.
wrf_resnow_floor.inc
wrf_snow_discount.inc
probe_inputs.txt
probe_outputs.csv
wrf_snow_probe
