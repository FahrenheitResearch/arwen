[package]
name = "grib1_bridge"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "gpuwm_preprocess_cpu"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
chrono = { version = "0.4", default-features = false }
grib-core = { path = "vendor/grib-core", default-features = false }
