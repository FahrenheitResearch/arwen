"""FTZ receipt toolchain: route inventory, GPU probe, claim census.

The package holds the measurement half of the arithmetic contract.  Nothing
here states what the hardware does; every statement is derived from a bit
table this toolchain records on the device.
"""
