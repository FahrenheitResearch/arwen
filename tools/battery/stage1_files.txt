# The release battery's stage-1 curated list.
#
# WHAT THIS IS
#   The set of test files the release battery runs as its stage-1 leg:
#   CPU-only (GPUWM_NO_LOCAL_GPU=1), fast enough to run on every cut, and
#   chosen to cover the paths a release most often breaks.  It is a
#   CURATED list, not a discovered one -- `pytest tests/` is a different,
#   much longer job, and stage 1 exists precisely because it is short.
#
# WHO CONSUMES IT
#   The per-assembly queue scripts (scratchpad q<NNN>_legs.ps1, "LEG S1").
#   Their $stage1 array is GENERATED from this file, not retyped:
#
#     $stage1 = @(Get-Content (Join-Path $CUT 'tools/battery/stage1_files.txt') |
#         ForEach-Object { $_.Trim() } | Where-Object { $_ -and -not $_.StartsWith('#') })
#
#   This header used to point at scratchpad q_TEMPLATE_stage1.ps1 for the
#   full leg.  That file is gone from every machine -- the same failure
#   this list closes (task #122), one level up.  Before this file existed
#   the list lived ONLY in those scratchpad scripts and was propagated by
#   copy from one assembly to the next; a lost scratchpad silently lost
#   every amendment ever made to it.
#
#   BEFORE RUNNING THE LEG
#   The battery runs in a throwaway worktree, which carries no build
#   output, so the rust binaries every real-artifact suite drives are
#   absent -- and those suites SKIP honestly rather than fail, so the leg
#   goes green with the rust half of the product untested.  Stage the
#   binaries first, and let the script prove they took:
#
#     pwsh -File tools/battery/provision_stage1.ps1
#
# FORMAT
#   One repository-relative path per line, forward slashes.  Blank lines
#   and lines whose first non-space character is `#` are ignored.  Order
#   is the order the battery runs them and is append-only by convention,
#   so the amendment history stays legible.
#
# AMENDMENT DISCIPLINE
#   An addition carries its REASON inline, on the line above it, naming
#   the task or the incident that earned the entry.  A removal is not a
#   tidy-up: say why the coverage is no longer needed, in the commit.
#   tests/test_stage1_manifest.py is the gate -- it checks every path
#   exists, rejects duplicates, and pins the two entries whose absence
#   caused a real miss.

tests/test_runplan.py
tests/test_nest_spawn_soil.py
tests/test_nocturnal_radiation_guard.py
tests/test_domain_wizard.py
tests/test_go_chain.py
tests/test_hrrr_prepared_background.py
tests/test_da_nowcast_presets.py
tests/test_da_nowcast.py
tests/test_da_sweep.py
tests/test_anisotropic_mixing_advisory.py
# tests/test_anisotropic_mixing_w_stability.py is the third of the four
# and is deliberately NOT on this list.  Every test in it is whole-module
# GPU-marked, so the CPU leg collected 0 of 2 while the line read as
# coverage; it was already listed on tools/battery/gpu_shard_files.txt
# SHARD 1a, which is the leg that actually runs it.  Removed 2026-08-28
# with tools/battery/no_silent_deselection.py, the guard that fails a leg
# when a file it was told to run contributes no tests.  This line stays
# so a reader who finds three of the four here does not conclude the
# fourth does not exist.
tests/test_les_tornado_attempt1_config.py

# 2026-08-09, the 1.6.0 exposed-mixing defect: the criterion and its
# advisory (the three entries directly above) had been on this list since
# they were written, and every shipped LES example was on the wrong side
# of the criterion anyway -- nothing walked configs/ and checked.  This
# is the file that walks it.  Placed with its three siblings rather than
# appended, against the header's append-only convention and deliberately:
# these four are one gate in four files, and a reader who finds three of
# them here would reasonably conclude the fourth does not exist.  The
# amendment history the convention protects is in this block.
tests/test_shipped_configs_mixing_stability.py

tests/test_doctor.py
tests/test_ftz_claim_consistency.py
tests/test_letkf_osse.py
tests/test_certification_capsule.py
tests/test_package_data_coverage.py

# The companion distribution split (2.5.0).  gpuwm-data carries the
# RRTMGP and Thompson table directories because the single wheel had
# reached 103.62 MiB against PyPI's 100 MiB cap.  Stage 1 gates it
# because both halves are cut and uploaded together: a version that
# drifted, or a moved table whose bytes changed, is a release-shaped
# failure and nothing else in stage 1 would see it.
tests/test_companion_distribution.py

# Task #121: the physics registry and its citation checker.  The 1.8.5
# battery was the first to inherit the checker, and stage 1 had no gate
# on the registry itself until this entry was added.
tests/test_physics_registry.py

# Task #122: the suites that gate registry-DERIVED artifacts.  During the
# 1.8.5 assembly this suite went red --
# test_ground_truth_receipt_regenerates_byte_for_byte, because
# docs/public/receipts/F2-ground-truth.json is regenerated from the
# registry and had not been landed with the registry edit.  Stage 1
# reported 0 failures on that same tree; only the lane-union leg caught
# it, and a lane union is assembled per cut, so it is not a gate.  A
# registry edit reaches these receipts on every cut, so the gate belongs
# here.
tests/test_physics_registry_composition.py

# ArWen 1.8.7: the render-on-first-committed-frame surface.  The run now
# renders the analysis frame as it lands rather than at finalize, which
# puts a new default-adjacent output on every run: the
# first_products_ready event, and a finalize that must digest-SKIP the
# frame the early render already claimed.  A regression here is silent --
# the run still finishes and the products still exist -- so it belongs on
# the list the battery runs every cut.
tests/test_first_products.py

# ArWen 1.8.7: the time-to-first-plot contract itself.  The fetch is
# ordered analysis-hour-first so the analysis frame can be committed
# early, and SHA256SUMS is pinned order-independent so that reordering
# cannot move a digest.  Both halves are contract, not implementation
# detail, and an ordering change would otherwise pass unnoticed.
tests/test_fetch_analysis_first.py

# Task #124: the radiation-bearing MYNN family.  Three shipped suites
# whose whole point is a property no other test measures -- MYNN that a
# night window may load -- and the property is easy to lose by accident:
# any edit to a switch row, a registry template or the nocturnal guard
# can silently put them back in the class the guard refuses.  The file
# also carries the control that the no-radiation siblings ARE still
# refused on the same window, so the claim cannot pass vacuously.
tests/test_mynn_radiation_profiles.py

# Task #124: the physics composition walk.  6530 combinations through
# gpuwm.experiment.build_experiment, and a receipt regenerated from the
# result and compared byte for byte.  It belongs on every cut for the
# same reason the F2 receipts do -- any admission edit in gpuwm/config.py
# or gpuwm/physics_compat.py moves the receipt, and a receipt that is not
# landed with the edit is a silent red on the next assembly.  It also
# carries the standing gate that every refusal names a selector and that
# doing what a refusal says reaches an accepted run, which is the pair of
# properties that found two defects the day it was written.
tests/test_physics_composition_walk.py

# Task #124: PHYSICS.md's reachability claims, derived from the objects
# that own them.  The page's maturity/reachability columns claim to be
# quoted verbatim from the registry, and they DRIFTED -- the revised MM5
# row still said "unreachable" after the registry had moved it to
# component-override, and the MYNN rows called the 5/5 pairing mandatory
# in a direction the loader does not enforce.  Both misled real readers
# into "the physics is hardcoded".  This pins the columns against the
# registry, the two quoted refusal messages against what
# build_experiment actually emits, and the implemented-unverified census
# against the registry's own count, so the page cannot drift back.
tests/test_physics_md_reachability_claims.py

# Task #124 verification sweep: the OTHER line-anchored receipt.  The
# census above (test_ftz_claim_consistency.py) has gated its artifact on
# every cut since 1.5.1, but its sibling never joined the list --
# tools/ftz_receipt/receipt/route_inventory.json pins a file:line and
# the exact source text for all 62 cupy kernel-construction sites, and
# only test_ftz_route_inventory.py regenerates it.  It was found stale
# AT integration/release-1.8.7, missing the two cp.ElementwiseKernel
# sites 6e9c690f0 added to gpuwm/core/dycore.py and gpuwm/core/moist.py:
# a perf lane moved code, the receipt was not regenerated, and no stage-1
# gate could see it.  Same failure mode as task #121's citation drift and
# as the census miss this sweep opened with, so it belongs on the list
# the battery runs every cut.
tests/test_ftz_route_inventory.py

# Task #125 (prov-wire): the provenance policy at the shared config
# load.  build_experiment now refuses a version disagreement, warns one
# line on a borrowed-but-agreeing version, and stays silent for a
# wheel; every front door inherits all three from that seam.  A
# regression is silent-green everywhere else -- the run still runs,
# labelled with a release that is not executing -- so the wiring proof
# runs on every cut.
tests/test_provenance_wire.py

# The green-on-nothing gate audit, 2026-08-09: the OTHER half of the
# radiation gate.  This list already carries
# tests/test_nocturnal_radiation_guard.py, which proves a bad radiation
# pairing is REFUSED -- and nothing that proves radiation RUNS.  A
# refusal-only gate is green on a tree where the radiation step has
# stopped producing heating: the refusal still fires on the bad config,
# every listed suite passes, and no stage-1 assertion anywhere would
# read a single Kelvin per second.  This file closes that half on the
# CPU: atmosphere -> stock Dudhia adapter -> the production
# PhysicsDriver radiation seam, asserting rthratensw > 0 and swdown > 0
# through the real driver, with the night arm (exact zero) beside it so
# the instrument is exercised in both directions.  10 passed, 2 skipped
# in 0.64 s under GPUWM_NO_LOCAL_GPU=1 -- the two skips are the
# @pytest.mark.gpu CUDA twins, which stage 1 is supposed to skip.
tests/test_wrf_legacy_radiation.py

# The 1.8.8 fidelity audit, round 1: the three binding laws' own suites.
# The eight-lane merge was landed on the strength of "FULL 28-file
# stage-1 battery green, 0 failed" -- a true statement about the curated
# list and NOT evidence that the laws landed intact, because every one of
# the laws' suites sat outside it.  Running the battery UNION the 39
# lane-touched files found 17 failures in 4 files, none of them on this
# list.  Three of the seven entries below were red at that moment.  A
# contract that gates a release belongs on the list the release runs, so
# the next cut cannot be told the same true-and-irrelevant thing.
#
# Law B, the renderer contract: gpuwm.render.renderer_refusal is the one
# decider at both seams (CLI and in-process), and this file carries the
# positive control -- the test that proves the gate can ever say yes.
# "A refusal that fires on everything is not a contract check, it is an
# outage" is its own docstring, and it was red at 5d6470308.
tests/test_render_rust.py

# Law A, the guard: a land surface with no longwave scheme is refused
# unless the experiment declares it.  This is the config-load half.
tests/test_radiation_off_land_surface_guard.py

# Law A, the receipt: gpuwm.runtime.downward_longwave_source, the line
# that tells a reader whether the GLW in their run was computed, declared
# or unused.  Guard and receipt read one classifier
# (downward_longwave_disposition) on purpose; listing only the guard is
# how they would be allowed to drift apart.
tests/test_downward_longwave_source.py

# Law C, the tokens: every shipped acknowledgement justified in its own
# file, and the nocturnal token never standing in for the constant
# longwave one.  Governance over configs/, so it goes red on a config
# edit that no code test can see -- which is exactly what happened at
# 5d6470308, where a frozen LES record missed the new union rule.
tests/test_shipped_acknowledgement_justifications.py

# Task #125's gate itself.  gpuwm/provenance_gate.py is new in 1.8.8 and
# it refuses EVERY front door when the executing tree disagrees with the
# installed metadata; the renderer bridge clause refuses a foreign
# engine.  A regression in either direction is a release-stopper -- too
# strict and no source checkout runs, too loose and receipts get stamped
# with a version that is not executing -- and the wire test
# (test_provenance_wire.py, listed above) proves the seam is called, not
# what it decides.
tests/test_provenance_gate.py

# The materializer's declared-physics contract: --physics-profile may not
# silently overwrite a value the config declares.  It publishes the
# experiment.toml every later stage binds by hash, so a regression here
# runs a different forecast than the config describes and still passes
# every downstream check.  Red at 5d6470308 on a cross-lane collision
# (materialize's assertion against glw's re-specified config).
tests/test_prepared_single_domain_forecast.py

# The HRRR single-domain route.  Thirteen nodes of this file were red at
# 5d6470308 on a real production hole -- eight shipped asymmetric
# profiles could not build an experiment on an all-daylight window, with
# no operator escape hatch, because the route synthesizes its config in
# code.  The file was outside the battery, so the assembly could not see
# it.  It is CPU-only and runs in about 5 s.
tests/test_hrrr_single_domain_benchmark.py

# The 1.8.8 fidelity audit, round 4: the two-layer radiation refusal, seen
# from the run route.  The 1.8.8 declaration guard fires at
# gpuwm.experiment.load_experiment and PRE-EMPTED this file's own route
# tests -- three of them went red at 48a88e3d5 because their fixture
# configs stopped loading, so the route layer they name went untested
# while the suite still looked like it covered it.  A layering change is
# exactly the class stage 1 cannot see from outside: the guard's own
# suite stays green, the route's suite goes red, and only running the
# route's suite says which layer moved.  It also carries the load-layer
# pin (token-less config refused before any route logic, preflight
# exit 2 against exit 1) so the ORDER is a test and not a comment.
tests/test_battery_route.py

# The 1.8.8 fidelity audit, round 4: the derived-doc renderer.  The FTZ
# statement blocks in PROVENANCE.md and docs/public/HARDWARE.md are
# GENERATED from tools/ftz_receipt/receipt/, and this suite is what
# compares the published bytes to a fresh render.  Its sibling
# test_ftz_route_inventory.py is already on this list and regenerates the
# receipt those blocks read -- which is how PROVENANCE.md came to publish
# "the 14 compile sites" against a receipt recording 16: the receipt was
# regenerated this release, the prose derived from it was not, and no
# listed suite compared them.  A receipt gate without its render gate is
# half a gate.
tests/test_ftz_render.py

# The 1.8.9 rebadge, and the reason there is one.  The wheel-staging gate
# in tools/build_rw_wps_release.py is the only thing that proves the
# standalone RW-WPS preprocessing wheel can be built and imported without
# CuPy, and it ran only at a release cut.  Two imports added during the
# 1.8.8 line pointed at modules that wheel deliberately does not carry;
# nothing said so until the cut, the tag was already spent, and the
# release became 1.8.9.  The WDM6 verifier's durability finding had
# already named this suite and the one below as ungated coverage.
tests/test_native_wrf_distribution.py

# Named ungated by the same durability finding.  It pins that every
# evidence axis a receipt publishes is one the readers agree on, which is
# the class of drift that only shows up when a document and the tool that
# derives it move in different commits -- exactly what stage 1 exists to
# catch before a cut does.  The WDM6 lane's round-2 verification named
# the same two suites independently, on the same reasoning a registry
# edit reaches both on every cut.
tests/test_evidence_axes.py

# THE 1.9 GATE'S CLASS-KILLER.  The composition walk above measures ONE
# gate -- gpuwm.experiment.build_experiment -- and the memory preflight is
# a second one that every real run reaches (gpuwm/core/model.py calls
# estimate_experiment unconditionally, and `gpuwm check` is that call).
# Its kernel-module tables are fail-closed, so a scheme the loader admits
# and the preflight has no row for is a scheme a user can select and
# cannot run.  Two shipped that way into the 1.9 assembly -- mp_physics=50
# (P3) and bl_pbl_physics=2 / sf_sfclay_physics=2 (MYJ + Eta) -- both
# recorded ACCEPTED in the walk receipt, both dead at `gpuwm check`.  This
# suite prices every composition that receipt accepts and allows a refusal
# only when it matches a DECLARED composition rule, so the next scheme
# that lands without a row fails here on the cut instead of in a user's
# first run.  ~4 s, CPU-only.
tests/test_composition_pricing.py

# Task #168, the lake-blockiness root cause.  Water temperature was chosen
# per cell between two differently-mapped fields, and the switch itself was
# the artifact; the assembly that replaced it had no stage-1 gate, because
# the defect shipped from 1.0 and was found by measuring a render rather
# than by a suite.  These tests pin the guarantees a revert would break:
# one provider per connected body, no ocean donor for a lake, no land donor
# for any water cell, a total WATER_TEMP_SOURCE, and wrf_compat reproducing
# the historical selector exactly.
tests/test_water_temperature.py

# Its neighbour: the mapped SST field stays METGRID.TBL's, fill and all,
# because the reconciler and the parity batteries read it expecting WPS.
tests/test_horiz.py

# Its counterpart: the declared-overlay path that replaces those water
# temperatures outright.  What it pins is CATALOG IDENTITY, not field values:
# that the optional key stays out of ExperimentConfig, that an absent overlay
# leaves the experiment fingerprint untouched, and that a declared one moves
# the input-catalog identity exactly when its bytes move.  A change to the
# DEFAULT water treatment does not move any of that, so this file is here for
# the overlay path's own refusals and loaders, not as a tripwire for the
# default.
tests/test_water_overlay.py

# The line-ending gate.  `.gitattributes` sets `* -text`, so a rewrite from
# LF to CRLF is a real byte change in files the product hashes for identity
# (_runtime_source_identity, the preparation proof's export-source set).
# Task #168 rewrote thirteen files that way with a scripted edit and the
# stage-1 battery stayed green through two full runs, because the gate that
# catches it was not in this list.  It is a CPU test and it takes a second.
tests/test_line_ending_stability.py

# The release-note gate.  It reads the NEWEST changelog section and fails on a
# branch name, an internal review verdict, a case identifier, or a development
# log pasted in whole.  Every lane writes into that section and nothing else
# checks it, so the first reader of a red here is whoever is cutting the
# release.  Task #168 turned it red and the stage-1 battery stayed green, the
# same blind spot the line-ending gate above closed.  It is a CPU test and it
# reads one file.
tests/test_release_notes_are_public_facing.py

# 1.9.1 D1, the second accepted-but-cannot-run instance in ONE release,
# one table over from the last one: mp_physics=9 shipped ACCEPTED by the
# loader and PRICED by test_composition_pricing, and could not BUILD its
# real-case workspace -- preflight's shape manifest had no mp=9 block, so
# the state builder's rebuilt() views had no backing.  This suite is the
# missing rung between "prices" and "steps": for EVERY mp value the
# config loader accepts it builds the shared dycore-state workspace from
# the preflight declarations and constructs the DomainState against it,
# then requires the full allocated symbol closure to be declared and
# restart-classified.  CPU-only (NumPy-backed state), under a second.
tests/test_mp_accepted_builds.py

# The tilestream port, and the reason the option surface is gated here
# rather than on the GPU shard.  [tiles] is a front-door table: it is
# read, validated and refused entirely on the CPU, and two of the ported
# commits exist because that door once let a misconfiguration through in
# silence -- the case-data route ignored the table outright, and
# mode = "auto" declined without saying so.  This suite is the
# parametrized refusal table for every key the option accepts, it is the
# conformance suite the physics registry does not cover (a runtime table
# has no WRF namelist counterpart and so takes no registry row), and it
# needs no card.
tests/test_streaming.py

# Spawn-at-trigger under [tiles], same port.  A nest that fires mid-run
# is default-adjacent surface: the streamed route has to decide whether
# the child fits resident and refuse rather than window it wrong, and
# that decision is CPU-side config resolution.  Listed separately from
# its sibling above because the two suites fail for different reasons
# and a reader who sees only one would not know the other exists.
tests/test_spawn_streaming.py

# The [tiles] gate list, from the same port.  The lane's gates are not
# pytest and nothing collects them, so a gate that stops existing -- a
# module renamed, a runner moved -- is invisible until a release cut
# tries to run the leg.  This is the cheap hermetic half of that
# coverage: it reads tools/battery/tiles_gates.txt, resolves every module
# against the tree, and pins that the consolidated gate and the tile_hook
# contract are BOTH listed, because the consolidated gate is periodic by
# design and was 233 PASS / 0 FAIL through the whole period the lateral
# boundary seam was broken.  No card, two file reads.
tests/test_tiles_gate_manifest.py

# The green-on-nothing preconditions in the same lane's gates, on the
# d998bb667 / 9b1b99289 incident: four release gates reported success when
# the thing they check was absent.  The ported gates carried all three
# shapes -- two empty carrier maps hashing equal and reporting bit-exact,
# a reassembly verdict of "not bad" that passes at checked == 0, and
# verdict lines that assert totality while stating no size.  This suite
# gates the fixes in both directions, with a positive control beside every
# refusal so a condition that refuses everything cannot masquerade as one
# that refuses the empty case.  numpy only, no card, 0.3 s.
tests/test_tiles_gate_preconditions.py

# The GLW seam between the same lane's harness and the release line's
# downward-longwave rule, which is the one collision the integration map
# predicted that no file diff could show: the gate's +Noah LSM rungs pair
# a land-surface scheme with radiation off, the GLW lane made that a
# refusal, and five gate cases raised it on the first run from the
# integrated tree.  The harness now declares the constant those rungs were
# always silently getting.  Gated here because the decision is which
# keyword argument gets passed and that happens before any allocation, and
# because it has to hold in BOTH directions -- a harness that declared
# unconditionally would overwrite a longwave scheme's own field.
tests/test_tiles_harness_glw.py

# The physics allocation inventory, added on the gate that caught it: the
# same [tiles] port moved a `cp.zeros` out of kf.update_trigger_history
# and into the new kf.ensure_trigger_history, updated the inventory for
# gpuwm/core/streaming.py, and left kf.py's row describing the old code.
# Stage 1 was green the whole time because this file was not on it; the
# union leg is what went red.  It is a pure AST scan of the physics
# modules -- no card, no cupy, under a second -- so there was never a cost
# reason for it to be missing, and the class of defect it catches
# (an allocation that moved, or a new one nobody priced) is exactly what a
# release most often breaks.
tests/test_physics_allocation_inventory.py

# The distribution's import closure, from the gate that found the wheel
# shipping gpuwm/core/streaming.py without the tilestream package it calls
# at about twenty sites.  Every one of those imports is function-local, so
# `import gpuwm` succeeded in a wheel that could not run [tiles] at all,
# and every gate passed because tools/battery/tiles_gates.txt says to run
# FROM THE REPOSITORY ROOT -- which puts the missing package on sys.path.
# The check is the general property rather than the one fix: the shipped
# package set must be CLOSED under importing.  AST walks plus one
# interpreter start.
tests/test_tiles_distribution.py

# Task #144: specified lateral boundaries through the multi-GPU path.
# The CPU-hermetic half of the forced decomposition: the non-periodic
# plan clamp, the seam census with per-side halo widths, the per-rank
# boundary-table windowing (real tables on true edges, inert on seams),
# the corner-rank data path, and every named refusal.  All 25 red on
# revert of tilestream/multigpu.py; the stepped bit-identity half is
# tilestream.test_forced_gate on the GPU shard.
tests/test_multigpu_specified_bc.py
# 2.0 blocker: the streamed twin died at model second 3620 because the
# carrier contract's produced-at ledger never rode the tile sweep's
# clock round trip -- fresh buffers consumed hour-N fields under
# build-time stamps.  CPU-hermetic, red on a revert of the carriers
# entries in tilestream/physics_inventory.py; the real 2 h streamed
# boundary crossing is the node regate's twin row on the GPU shard.
tests/test_streamed_carrier_freshness.py
# Drew's 2026-08-12 ruling ("probably we would want tests right") made
# the Rust crate tests a release gate, at tools/battery/cargo_gates.txt.
# This is the gate on that list, and it is on stage 1 rather than only
# on the cargo leg because it needs no Rust toolchain and no card: it
# reads the list against the two workspaces' Cargo.toml members in both
# directions, so a crate that arrives in `members` without an entry --
# which is how a crate lands already ungated -- goes red on every
# commit rather than at a cut.
tests/test_cargo_gate_manifest.py

# The release machinery's own gates, earned by the 2.1.0 assembly.  The
# tenth bundled artifact is vendored and deliberately unstamped, proved
# by its contract marker instead; commit 6775e7a0a taught that to the
# packer and not to tools/verify_release_artifacts.py, so the verifier
# would have refused the bundle it was handed in the prepare job -- mid
# cut, after the tag, with seven of this suite's cases already red on the
# branch and no leg looking at them.  Release-machinery suites are
# exactly the ones a cut cannot afford to discover late: they are
# CPU-only, they build their fixtures through the real packer and the
# real snapshot builder, and they are the difference between a defect
# found on the branch and a defect found between the tag and PyPI.
tests/test_verify_release_artifacts.py
tests/test_release_snapshot_front_door.py
tests/test_release_snapshot_machine_paths.py
tests/test_release_snapshot_modes.py

# ---------------------------------------------------------------------------
# The 2026-08-13 test-estate audit's stage-1 amendments.
# ---------------------------------------------------------------------------

# THE GATE ON THIS LIST WAS NOT ON THIS LIST.  test_stage1_manifest.py is
# what stops a tidy-up dropping an entry -- it checks every path resolves,
# rejects duplicates, and pins the entries whose absence caused a real miss
# -- and it ran only in the full-suite leg, which is assembled by hand each
# cut.  The audit found it by parsing the list rather than grepping it: a
# grep -F reported it as present, because the header names it at line 33,
# inside a comment.  Any membership check against this file must ignore
# comment lines, exactly as the header's own consumer snippet does.
# 0.2 s, 138 tests.
tests/test_stage1_manifest.py

# The privileged publish workflow, executed.  This suite runs the literal
# `run:` blocks of the publish workflow against fake GitHub and PyPI, and
# three of its four commits are publish fixes: stranded assets on a re-run,
# PyPI index-lag backoff, invented refusal gates.  It gates the window
# between the tag and PyPI, which is the one window a cut cannot route
# around -- by the time a defect there fires, the tag already exists.  It
# sat on no leg.  The audit calls it the single most consequential addition
# to this list, and at 23 s it is the most expensive one; it is worth it for
# the same reason test_verify_release_artifacts.py is.
tests/test_publish_workflow_state_machine.py

# Three more release-machinery suites on no leg, matching the 2.1.0
# rationale above verbatim: CPU-only, fixtures built through the real tools,
# and the difference between a defect found on the branch and a defect found
# mid cut.  1 s, 0.4 s and 2 s respectively; test_doctor_da_eigensolver.py
# carries two field failures behind it.
tests/test_verify_source_bridge_pins.py
# The other half of the pins story: the source tree must stay unpinned
# (above), and no WHEEL may be built while it is (below) -- the 2.5.0
# candidate was built with the pin step skipped and installed on a clean
# home as FAILED bridges.  ~4 s; also on always_files.txt.
tests/test_wheel_pin_gate.py
tests/test_doctor_nexrad.py
tests/test_doctor_da_eigensolver.py

# Obs-DA was 5 of 63 files on this list: 58 files and about 1,900 tests
# never ran on a cut, against a lane that is 197 s in total and 48 s
# excluding its two big files.  These are the cheap high-value pins the
# audit names -- about 12 s combined here, 20 real defects between them --
# and adding them is a better use of an amendment than any deletion in the
# audit's pruning section.  Radar ingest and the radar grid; the LETKF
# solver and its chunk sizing; the observation operator and its moments; the
# v1.2 integration seam; and the ensemble-product hardening.
tests/test_obs_nexrad.py
tests/test_obs_radar_grid.py
tests/test_letkf.py
tests/test_letkf_chunk_sizing.py
tests/test_da_obsop.py
tests/test_da_moments.py
tests/test_v12_integration.py
tests/test_enprod_hardening.py

# The gate on the gate-PLACEMENT class, from the same audit's G1.  A
# module-level skip below a test definition silently takes the tests above
# it: tests/test_first_products.py had one at line 568 and collected zero of
# its 23 tests on every cut since 1.8.7, while sitting on this list.  Two
# more instances were found by AST sweep.  This file walks every test module
# in tests/ and tilestream/ and fails on the shape, so the next one cannot
# land quietly.  2 s.
tests/test_module_skip_placement.py

# The fast-fix selector's own gate.  tools/battery/fastfix.py decides which
# suites a change must run before it is published, which makes it a thing
# that can be silently wrong -- the failure mode the whole audit is about.
# This pins the audit's validation commit (6e9c690f0 must still select
# tests/test_pd_advection.py), that a non-Python change falls back to the
# ALWAYS list rather than to nothing, that the selection stays a selection,
# and the manifest hygiene on tools/battery/always_files.txt.  38 s, and it
# also gates tools/battery/gpu_shard_files.txt.
tests/test_fastfix_selector.py

# ---------------------------------------------------------------------------
# The 2.2 train: lane/ingest-rust-2, appended to the restructured list above.
# ---------------------------------------------------------------------------

# Round-2 survey candidate 1: the projected-source horizontal operator
# now runs in the Rust bridge, and the NumPy mirror it replaced is the
# pinned authority for every nested HRRR preparation ever run.  This is
# the bit-identity gate on the raw uint32 view -- both `oned` sweeps,
# the zero/tiny sentinel, the clamped stencil edges, the fraction end
# points, and the host-versus-CUDA missing-value predicate.  Red on a
# revert of either the predicate or the sentinel in
# tools/grib1_bridge/src/lib.rs; skips cleanly when the bridge is not
# built, which is why it also asserts the loud fallback.
tests/test_hrrr_projected_operator.py
# Round-2 survey free win: the fetch engine used to degrade to the
# Python transport in silence on the branch every install without the
# bridges bundle takes, and that transport has no whole-file mode -- a
# measured ~16x tax paid by default.  Covers the warning at selection
# time, the engine_selection receipt field, and the doctor line.
tests/test_fetch_engine_degrade_guard.py

# ---------------------------------------------------------------------------
# The 2.2.0 cut: the user-door gate's five findings against the streamed
# front door, and the checks that would have caught them.
# ---------------------------------------------------------------------------

# THE CHECK THAT WAS MISSING.  Every streaming gate the release had
# compared VALUES of fields both files carried; the defect was a field
# only the RESIDENT file carried, so a streamed run published 73 of 74
# variables with forecast validity PASS.  This is the instrument for
# streamed-versus-resident FRAME FIELD-SET equality, validated in both
# directions -- a pair it must call identical, and four seeded
# corruptions it must catch (a dropped field, an invented field, one
# flipped float bit, one changed provenance attribute), because an
# equality checker that always answers "equal" passes every parity gate
# ever written.
tests/test_streamed_frame_parity.py
# The admission gate, at the front door.  `gpuwm go` refused a
# 550-class config with [tiles] on the strength of a RESIDENT memory
# estimate -- the one configuration class streaming exists to enable,
# refused by default, with --no-memory-gate printed as the remedy.
# Drives memory_gate with a probe standing in for the card: the
# streamed config must be admitted, the same domain without [tiles]
# must still be refused, and a domain too big even streamed must be
# refused in the tile-and-window numbers a user can act on.  Also pins
# that `gpuwm check` stopped telling users the feature does not work.
tests/test_streamed_admission.py

# Task #204, the dead ERA5 direct door.  The wizard's own emission --
# the exact file `gpuwm domain --source era5` writes -- was refused by
# the ERA5 adapter as "does not have a table 'case_data'" while the
# table sat in the file, and no gate anywhere fed a wizard emission to
# the adapter that must consume it.  This is that gate, for EVERY
# source (era5, gfs, hrrr): real wizard, real adapter seam, through
# config validation, CPU-hermetic.  It also pins the refusal-message
# class the incident exposed: present-but-unconsumed is a caller
# routing defect, never "absent".
tests/test_wizard_adapter_roundtrip.py

# Task #206, the crashing shipped profile.  The composed
# Thompson/Shin-Hong suite died on a bare 12 km case because the
# scheme's PASSENGER TKE diagnostic went non-finite and the driver
# validated it with the same fatality as the tendencies; stock WRF ran
# the identical initial state to completion.  This suite pins the
# mechanism through the float32 WRF authority (the chain can fail while
# every tendency holds), the repair policy in both directions (passenger
# repaired and floored, consumed outputs still fatal, first-invalid
# ordering kept), and the floor's equality with WRF's own cold start.
# CPU-only, under a second; the failure it guards is a crash in a
# user-selectable profile.
tests/test_shinhong_composed_tke_guard.py

# The upstream half of the same class: the ONLY input that reproduces
# the task-#206 crash signature is entry TKE at or below zero (or
# non-finite) reaching the chain, so the driver re-asserts WRF's
# shinhonginit floor at the seam, in place, said once.  This suite pins
# the hazard class through the float32 authority (zero/negative/NaN in,
# passenger pair non-finite while every tendency holds), the driver's
# heal (degenerate entries reach the launch AT the floor, healthy
# values bit-untouched, the identity fast path allocates nothing), and
# the advisory latch.  CPU-only except one gpu-marked end-to-end
# injected-cliff micro-run.
tests/test_shinhong_entry_floor.py

# 2.3 streamed-child lane: the CPU-hermetic half of the resident-parent +
# tile-streamed-child corridor -- the frame rule and its slice-rule
# composition, the coupler's frame pull off the child's store (real
# host-mirror seam, with the unpublished-store negative), the per-tile
# packed rolling-table windows with the launch-time generation reload and
# its disarmable stale-tables control, and the per-domain road walk's
# budget arithmetic and receipts.  Deliberately importable without cupy.
# The stepped bit-identity half is tilestream/test_streamed_child.py on
# the GPU shard.
tests/test_streamed_child.py

# The stream-attach lanes: the store-direct initialization road.

# Which INITIALIZATION ROAD a streamed forecast takes, and the pricing rule
# `--stream-init auto` decides it on.  On this list because the rule decides
# whether a run lives or dies before it allocates anything, and because the
# way it failed once was silent: it priced the resident road from the prepared
# cache's serialized-state manifest, called a 6.45 GiB fit at 1024x1024, and
# the road it chose then died with 16 276 726 272 B allocated.  Every
# assertion here is arithmetic over integers with no card anywhere, and the
# 1024 case is pinned to the byte in both directions, so a return to the
# manifest-only rule goes red on every commit rather than at a cut.
tests/test_stream_init.py
# The full-state health gate over a streamed domain's pinned HOST store.
# CPU-hermetic by construction -- the whole point is that a gate which used
# to need a domain-shaped DEVICE state can be held to the same per-field
# bounds where the domain actually is.  Each rule class is tripped in the
# store's LAST row, so a gate that quietly read one slab and reported the
# domain goes red here rather than after a forecast.
tests/test_store_health_gate.py

# The two front-door nesting legs, graded without a card (2.3.0 round 2,
# task #223).  The legs themselves are scripted GPU gates in
# tools/battery/tiles_gates.txt -- they drive `gpuwm run <config> --outdir
# <dir>` and compare whole forecasts, which this list cannot express.  This
# entry is the CPU half: it holds the two entries' LEG= environment (without
# it the battery would run leg A twice and report two passes), keeps a card
# budget out of the committed case files, and pins the two SHAPES, which are
# written down nowhere else in the tree.
tests/test_tiles_door_legs.py

# Five files central to the 2.3.0 riders that sat on NO list (found by the
# 2.3.0 cut gate, the same "a gate nobody can enumerate" class that earned
# test_streaming_clock_arming.py its registration this round).  All five are
# CPU-hermetic and about 24 s combined.
#
# test_doctor_cuda_headers.py holds BOTH tombstone regression guards -- the
# guard against the very defect the doctor rider fixed ran on no leg -- plus
# the docs guard added at the cut, after the release was found shipping a
# public quickstart telling CUDA-12 readers to install the exact wheel names
# the code had just stopped printing.
tests/test_doctor_cuda_headers.py
# The wrf-rust pin rider's core guard and its two consumers.  The pin moved
# from four copies of `== "0.2.35"` to one window in gpuwm/science_core.py;
# these are what refuse a return to a hard equality pin, and what prove a
# score file reports the version that ACTUALLY read the run.
tests/test_obs_model_source.py
tests/test_flagship_tools.py
tests/test_obs_cross_reader.py
# The GPU shard list's own gate: it checks every path on
# tools/battery/gpu_shard_files.txt resolves and every entry is real.  It is
# named in that file's comments as "the gate" and was an entry on no list,
# so the instrument that keeps the shard list honest was itself unrun.  CPU
# only -- it reads the list, it does not touch a card.
tests/test_gpu_shard_manifest.py
# 2.3.3: 2.3.2 shipped worldwide 30 m terrain that no documented install
# could reach -- rasterio/pyproj were an extra [all] excluded and no
# quickstart named.  Both halves were individually correct; only holding
# the docs against the extras that exist catches it.
tests/test_docs_extras_agree_with_code.py
# 2.3.3: the same defect's ORDER half -- the dependency check ran after
# the tile fetch, so a missing library cost 160.7 MiB and a raw traceback.
tests/test_highres_geog_stack_refusal.py
# 2.3.3: `gpuwm static` demanded a forcing GRIB and Vtable it never opens,
# which put a met download in front of every terrain build.
tests/test_static_needs_no_met_inputs.py
# 2.3.3: four refusals that were tracebacks, false statements, or absent --
# the DA polygon message that named a file which was on disk, the case that
# needs a bundle nothing ships, and the two LES modules whose --help died
# on a config that is in no wheel.  CPU only.
tests/test_reachability_refusals.py
# 2.4.0: every gate this release's seven lanes added, plus the three
# pre-existing files their fixes rewrote.  They are here for the reason
# the list exists: each one is a claim about a FRONT DOOR -- what doctor
# reports, what a refusal offers, what the bundle carries, what a
# document says a command takes -- and the merge that assembled them
# broke sixteen of these rows at once while every lane's own tip was
# green.  Stage 1 is where that gets caught before a tag is spent.
# All CPU only.
tests/test_certify_reachability.py
tests/test_certify_compile_platform.py
tests/test_dependency_declaration_parity.py
tests/test_verification_recipe_flags.py
tests/test_doctor_extras.py
tests/test_doctor_route_honesty.py
tests/test_cli_capability_refusals.py
tests/test_obs_front_doors_are_shipped.py
tests/test_render_pyshp_front_door.py
tests/test_bridge_source_rev_stamp.py
tests/test_obs_battery_ingest.py
# The European radar route, decoder through front door.
tests/test_obs_cli_frontdoor.py
tests/test_obs_opera.py
tests/test_obs_radar_sites.py
tests/test_obs_superob_odim.py
tests/test_obs_surface_networks.py
tests/test_obs_sweeps_odim_v3.py
# Not a 2.4.0 addition: it predates this release and was never on the
# list, and its control arm inherited PYTHONSAFEPATH from the very law
# that says how to run it, so it failed whenever it was run correctly.
# Fixed here, and listed so it stays fixed.
tests/test_chain_stage_env.py
# 2.4.1: the publish workflow's own `test` job list.  It ran on a runner
# with no GPU extra and refused v2.4.0 on two rows this list did not
# carry, and the tag was already public.  Stage 1 on a development box
# cannot see those rows on its own -- GPUWM_NO_LOCAL_GPU=1 does not
# uninstall CuPy, so `capabilities.is_installed("cupy")` is still true
# here and the refusals that fire only where CuPy is ABSENT never fire.
# Listing the files is half the fix; the other half is the checklist item
# that runs them with no GPU extra installed.
tests/test_cli.py
tests/test_bridge_fetch.py
tests/test_table_fetch.py
tests/test_source_adapters.py
tests/test_namelist_compat.py
tests/test_bridge_bundle_adopt.py

# 2.5.0: the arbitrary-input door.  This release's headline is a pipeline a
# user drives from their own script -- their Vtable, their NetCDF, their
# namelist -- and the two suites that gate it were not on this list.  A
# guard added mid-cycle (mapped_authoring._require_declared_unit_scale)
# then refused this project's OWN shipped GFS and ERA5 authorities on
# land_fraction and volumetric_soil_moisture, which killed every compile
# route into the door, and stage 1 reported zero failures on that tree.
# Ten node failures, in the headline feature, invisible to the release
# gate.  Both files are fast and CPU-only, so they belong here.
tests/test_adapt.py
tests/test_mapped_authoring.py

# 2.5.0 integration: the nine suites that gate THIS release's headline
# features and its cut mechanics.  Each one was written by a lane that
# could only run it against its own branch; none of them was on this
# list, so the battery that decides whether a cut may proceed could not
# see the release gates themselves.  Two concrete holes this closes:
# the packaging change that puts configs/ in the wheel (the LES cases
# are unreachable from a plain `pip install` without it) and the
# equivalence gate that keeps `gpuwm go` a composition of `gpuwm sim`
# rather than a second implementation -- the latter BROKE on merge and
# was caught only because integration ran the full suite.
# All nine are CPU-only and fast; measured together at 163 tests.
tests/test_release_version_declaration.py
tests/test_publish_dist_shape_agreement.py
tests/test_configs_are_packaged.py
tests/test_stage_seams.py
tests/test_progress_log.py
tests/test_render_layout.py
tests/test_chain_deep_ladder.py
tests/test_les_mixing_default.py
tests/test_dual_run_screen.py

# ---------------------------------------------------------------------------
# 2.5.0 release blocker #5: the tilestream pytest suites join the battery.
# ---------------------------------------------------------------------------
# tilestream/ carries 13 pytest suites (312 tests) beside its `python -m`
# script gates, and NO list ran any of them: pyproject testpaths said
# ["tests"], this file said tests/ only, and the gpu shard likewise -- so
# 11 failures and a collection error (test_hoststore.py::test_pinning had
# NEVER executed under pytest) sat invisible until the gating pass measured
# them.  tilestream/conftest.py now extends tests/conftest.py's GPU-marking
# discipline over the tree, so under this leg's GPUWM_NO_LOCAL_GPU=1 the
# device halves skip and what runs is CPU arithmetic.  Entries below are the
# files MEASURED to carry live coverage in that mode (Windows cut box,
# 2026-08-16, wall seconds per isolated pytest invocation in parentheses).
# NOT listed, deliberately: test_overlap.py and test_tile_hook_contract.py
# -- their modules' main() runners are already per-cut cpu entries in
# tools/battery/tiles_gates.txt and run MORE than their pytest half, so a
# listing here would double-run the lesser form; test_gather.py,
# test_graphcap.py, test_hoststore.py, test_exchange_pack.py -- 100% (or
# all-but-none) skipped in this mode, they are the gpu shard's.
#
# The tile/window geometry contract and its validate_plan checker (2.3 s).
tilestream/test_spec.py
# The ring-arena dependency model: saved bands, WAR deps, patch sources (1.8 s).
tilestream/test_rings.py
# The planner: rungs, budgets, refusal attribution (geometry vs vram, the
# scaling lane's smaller-arm CannotPlan inversion is pinned here), 42 CPU
# tests; its two device tests skip here and run on the gpu shard (9.9 s).
tilestream/test_autoplan.py
# The [tiles] admission story on the release line: estimate mode-blindness,
# the run/tree routes streaming instead of refusing, gpuwm go admitting
# mode='on' pre-fetch, and the OPEN two-VRAM-models defect pin (3.6 s).
tilestream/test_ledger_gate.py
# The prepared-store serialization surface, 128 tests, numpy only (1.9 s).
tilestream/test_prepared_store.py
# The store-direct frame road's CPU half (2.5 s).
tilestream/test_store_direct_frame.py
# hoststore.host_memory portable on Windows -- the box THIS leg runs on; the
# suite lives outside test_hoststore.py precisely so the cpu leg can see it
# (that module's helpers import cupy, so all of it skips here) (1.8 s).
tilestream/test_hostmem.py

# Task #154 (MPAS physics seam counterparty): the column-batch contract
# is a cross-program API an external MPAS core builds against.  This
# suite pins the published name, the orchestration-identity leaf-wrapper
# refusal, the cadence law and the whole pre-device refusal surface.
# ~0.4 s, CPU-only; the behavioural half is GPU-marked and runs on the
# rented card.
tests/test_mpas_column_batch.py

# The NOAHMP_GLACIER port (MPAS collaborator, real x4 case): the
# CPU-hermetic half pins the transcription's own WRF balance gates, the
# dispatch partition, the 0.02-vs-0.5 threshold boundary, the
# native-XLAND contract and the parameters' refusal surface.  ~0.5 s,
# CPU-only; the CUDA/host bitwise parity and the full-seam behaviour
# are GPU-marked in tests/test_noahmp_glacier_gpu.py on the rented card.
tests/test_noahmp_glacier.py

# Spectral verification v2 (additive lane beside the frozen v1 metric): the
# CPU-only controls for physical wavelength bands, amplitude-vs-phase error
# separation, rotational/divergent wind KE, fail-closed preregistration and
# self-hashed receipts, calibrated-only gates, and receipt-bound plotting.
# The v1 pin in tests/test_spectral.py is untouched and stays the frozen one;
# these two suites are what would catch a v2 pin or band rule moving silently.
tests/test_spectral_compare_v2.py
tests/test_spectral_receipt_v2.py

# The 2.5.0 RC verify (2026-08-18) found this suite priced in
# tools/battery/durations.json but on no leg's list, so "no case names in
# generic code" -- one of the project's binding laws -- had no stage-1
# gate at all.  The class of breakage is a case token reaching defaults,
# core or a generic identifier: it killed a prior project, it is the kind
# of thing a source-adding lane introduces without noticing, and every
# cut ships whatever is in those files.  1.0 s, CPU-only.
tests/test_case_token_leakage.py

# The local twin of publish.yml's `test` job.  The class of breakage:
# the replay builds its own venv, CI's 3.11 interpreter ships setuptools
# and a 3.12+ venv does not, and two of the job's own packaging tests
# REFUSE without it -- so on any modern box the replay went red on its
# harness while the job it replays was fine, and a harness whose verdict
# is about itself cannot be pointed at a public snapshot before a cut.
# The venv-building row is network-gated and skips by default; what runs
# here is the premise (the job's files need setuptools) and the seed
# list.  <1 s, CPU-only.
tests/test_ci_test_replay.py

# Three 2.5.1 headline features whose suites were never enumerated here.
# The class of breakage is the same for all three and it is the one this
# release exists to stop: a feature ships, its door is the thing users are
# told to reach for, and the gate that decides whether the release is
# green never runs a line of it.  A regression in any of them reaches a
# user before it reaches a test.
#
# [output] variable selection: the writer trims wrfout by preset or by
# list, and the plan-time dependency warning is what stops a trimmed
# history silently costing a render product.  Untested here, a selection
# that drops a field a product needs becomes a render failure at the far
# end of a run instead of a warning at the near end.
tests/test_history_selection.py

# The advective forcing pair: RTHFTEN/RQVFTEN leave the dycore for the
# cumulus driver.  462 lines proving the lane WRF feeds and we used not
# to.  Untested here, the pair silently reverts to zeros and Grell-Freitas
# runs on half its forcing -- which is exactly the defect the lane closed,
# and it would come back invisibly.
#
# tests/test_dycore_advective_forcing_export.py IS THAT GATE AND IT WAS NOT
# RUNNING.  Measured 2026-08-28: every test in it is whole-module GPU-marked,
# so this leg collected 0 of 9 and the entry above read as coverage that was
# never taken.  It was on no GPU list either, so those nine tests ran on no
# leg at all.  Moved to tools/battery/gpu_shard_files.txt SHARD 1a, which is
# where a whole-module GPU-marked file belongs; the reason text above is kept
# here because it is the argument for the coverage, and it now points at the
# leg that takes it.

# The speedrun door and its signed record.  Untested here, a course that
# no longer runs, or a record whose signature stops verifying, is found by
# whoever tries to reproduce a published number rather than by the gate.
tests/test_speedrun.py

# The 2.5.4 nest wave, verified on node-2 hardware AFTER stage 1 went
# green.  Both defects that leg found were invisible here for one reason:
# none of these three files was in this list.  A moved nest lost its
# surface-radiation carriers and refused at the first move under shipped
# radiation defaults; a prepared-tree run with a live follower could not
# checkpoint.  Both are front-door paths on headline 2.5.4 features, and
# both would have reached a user before they reached a test.
tests/test_restart.py
tests/test_prepared_domain_tree_forecast.py
tests/test_relocation_radiation_carriers.py

# 2026-08-27, lane/wif-climatology: the mp=28 WIF aerosol-climatology
# ingest (QNWFA_QNIFA_SIGMA_MONTHLY.dat -> nwfa/nifa/nwfa2d/nifa2d),
# oracle-measured against WRF-4.7.1 real.exe on the summer reference
# case.  CPU-only, synthetic fixtures, sub-second; pins the fail-closed
# admission around wif_climatology_path so the default path cannot move.
# 22 passed, 0.42 s.
tests/test_wif_climatology.py

# 2026-08-27, lane/wif-rust: the equivalence gate between the Rust WIF
# ingest engine (gpuwm_wps_intermediate_read, the cyclic bilinear, the
# pre-existing vert_interp entry) and the NumPy oracle-of-record whose
# numbers were bound against WRF-4.7.1 real.exe.  Skips itself when the
# CPU bridge is absent; otherwise sub-second on a small global fixture.
# Holds decode/horizontal/surface bit-exact and the vertical residual
# inside the oracle's own 1.032e-05 margin, so a bridge change cannot
# move mp=28 climatology numbers unnoticed.  6 passed, 0.49 s.
tests/test_wif_climatology_equivalence.py

# 2026-08-27, integration/engine-258-clean: the coverage the two lanes
# above and the RUC soil-geometry lift NEVER put on any list.  Found by
# asking the question the 2.5.8 membership audit was written to ask --
# "which new test file is on no list?" -- of the extracted engine work
# rather than of the campaign it was extracted from.  Nine files, and
# every one of them gates a front-door path:
#
#   test_ruc_source_matrix          17 passed  0.74 s   every admitted
#       soil source x both geometries, mapped layer route at 0 ULP
#   test_soil_layer_geometry        27 passed  0.67 s   ZS/DZS at 6 and
#       9 levels against real.exe, including DZS(7)=0.4999999
#   test_ruc_soil_wiring            23 passed  0.60 s   the ingest seam
#   test_physics_compat             11 passed  0.38 s   the
#       sf_surface_physics=3 admission arms
#   test_wif_dataset_contract        9 passed  0.40 s   the dataset pin
#   test_physics_md_aerosol_claims  20 passed, 2 skipped  1.50 s  the
#       public physics page's aerosol claims against the code
#
# 107 CPU assertions for about 4.3 s.  Measured one file at a time under
# GPUWM_NO_LOCAL_GPU=1, which is this list's own rule.
#
# TWO DELIBERATE OMISSIONS, so the gaps are visible rather than silent:
#   tests/test_ruc_runtime.py measured 24 SKIPPED on CPU -- it is device
#     work and belongs in gpu_shard_files.txt, where it now is.
#   tests/test_ruc_admission.py is held out because it carries a
#     PRE-EXISTING failure inherited from this branch's base: its
#     assertion names two RUC templates and gpuwm/physics_registry_v2.json
#     has carried three since before the base, the branch and the source
#     branch tip alike (the test function is byte-identical at all three).
#     Putting a known-red file on a per-cut list buys a red battery, not a
#     fixed test.  It goes on this list in the commit that resolves which
#     of the two -- the third template or the expectation -- is wrong.
tests/test_ruc_source_matrix.py
tests/test_soil_layer_geometry.py
tests/test_ruc_soil_wiring.py
tests/test_physics_compat.py
tests/test_wif_dataset_contract.py
tests/test_physics_md_aerosol_claims.py

# 2026-08-27, carrying the RUC column {6, 9} lift and the mp=28 aerosol
# source receipt onto this branch: nine more files that were on no list.
#
# The RUC estate was almost entirely unlisted.  The 2.5.8 membership audit
# above put four of its CPU files here; these are the rest, plus the two
# gates the {6, 9} lift brought with it and the one the aerosol receipt did.
#
# MEASURED on this branch, one file at a time under GPUWM_NO_LOCAL_GPU=1,
# which is this list's own rule:
#   test_ruc                          60 passed  16.47 s  the column itself
#   test_ruc_nzs_tier                 39 passed  27.49 s  the RUC_NZS macro
#       tier and the host/device soil-count resolution
#   test_ruc_column_batching          11 passed  16.56 s
#   test_ruc_contract                 15 passed   0.41 s  including the new
#       admitted-set assertion over WRF's {6, 9}
#   test_ruc_shoreline_soil_category   5 passed   0.43 s
#   test_ruc_soil_ingest              11 passed   0.46 s
#   test_physics_dispatch             33 passed   0.58 s  where the retired
#       six-level blocker's replacement assertions actually live
#   test_build_registry               82 passed  65.84 s  the gate holding
#       tools/build_registry.py and gpuwm/physics_registry_v2.json
#       byte-identical.  Slowest entry here by an order of magnitude, and
#       it earns it: it was RED while this work was being carried, because
#       the builder had learned to say six levels and the tracked registry
#       had never been regenerated.  A generator gate nobody runs is how a
#       document and its source drift apart in silence.
#   test_aerosol_source_report        26 passed   1.26 s  the mp=28 aerosol
#       source receipt, whose subject is an ABSENCE: a scheme with no
#       aerosol number fields must gain NO report key, and an mp=28 run
#       that fell back to the analytic profile must gain a loud one.
#       Untested, those two collapse into one shape and a WSM6 receipt
#       becomes indistinguishable from a run that silently had no dataset.
#
# 282 CPU assertions for about 2 min 10 s.
#
# TWO MORE DELIBERATE OMISSIONS, measured rather than assumed, and both for
# the reason the pair above already gives: a known-red file on a per-cut
# list buys a red battery, not a fixed test.
#
#   tests/test_ruc_admission.py stays off, and the held-out note above
#     still applies word for word.  Measured on this branch: the eleven-file
#     RUC suite ran 1 failed, 250 passed BEFORE this work and 1 failed, 257
#     passed after -- the same single failure both times, and it is this
#     file's template assertion.  The registry carries three RUC-selecting
#     templates and the test pins two.  The {6, 9} lift neither caused it
#     nor fixed it.
#
#   tests/test_mp8_frozen.py stays off, which is the harder call, because
#     this work MOVES FROZEN_MODULE_DIGESTS['ruc'] and a frozen digest
#     nothing checks is not a freeze.  Measured here at 2 failed, 19
#     passed, 1 skipped -- and identically so on this branch's tip BEFORE
#     any of this work, with the same nine drifting modules: diagnostics,
#     kf, nest, rrtmg_lw, rrtmg_sw, rrtmgp_gas, rrtmgp_mcica, rrtmgp_rte,
#     ysu.  'ruc' is in NEITHER drift set, so the pin this work moves is
#     correct and the red belongs to nine translation units frozen against
#     bytes from somewhere else.  It goes on this list in the commit that
#     re-pins them, not in this one.
tests/test_ruc.py
tests/test_ruc_nzs_tier.py
tests/test_ruc_column_batching.py
tests/test_ruc_contract.py
tests/test_ruc_shoreline_soil_category.py
tests/test_ruc_soil_ingest.py
tests/test_physics_dispatch.py
tests/test_build_registry.py
tests/test_aerosol_source_report.py

# FOUR FILES THIS BRANCH EDITS THAT RAN ON NO LIST.  Found by the rule
# SHARD 1f and 1g of gpu_shard_files.txt already apply here -- ask which
# file this work TOUCHES is on no list -- and then measured one at a time
# under this list's own GPUWM_NO_LOCAL_GPU=1 rule.
#
# THE CONCRETE BREAKAGE, and it is not hypothetical: it had already
# happened when this section was written.  The MYNN mixscalars lift made
# bl_mynn_mixscalars=1 an ADMITTED value on the device DMP mass-flux
# entry point.  Two tests asserted the old refusal.  The tendencies one
# was updated with the lift; its DMP-MF twin in tests/test_mynn_pbl_gpu.py
# was missed, and went red.  Nothing caught it for the length of two
# integrations, because that file is on no list -- so the estate's answer
# to "did the mixscalars lift break its own admission tests?" was silence.
# A test file that co-moves with a change and runs nowhere is a test file
# that only reports on the day somebody happens to run it by hand.
#
#   test_mynn_pbl                     60 passed              6.4 s
#       The CPU twin of the file that went red, and the reference the
#       device lane is judged against.  Its own mixscalars rows are the
#       ones that stayed correct, which is why the divergence was
#       invisible: the CPU half agreed with the code the whole time.
#   test_mp28_runnable      67 passed, 10 skipped            0.8 s
#       The mp=28 run door, which the aerosol-source default moved.
#   test_real_init          38 passed, 12 skipped            1.3 s
#       gpuwm.ingest.real is where the ONE resolver is called and where
#       the aerosol receipt is built; 284 changed lines here ran on no
#       list.
#   test_config_freeze                 4 passed              0.5 s
#       Holds tests/data/config_freeze_golden.json against RunConfig.
#       This branch adds two knobs to both, so the file and its golden
#       moved together with nothing on a list checking that they did.
#
# The device half of the same gap -- tests/test_mynn_pbl_gpu.py itself --
# is NOT here: measured 30/30 SKIPPED under GPUWM_NO_LOCAL_GPU=1, so on
# this list it would report green on nothing.  It goes on SHARD 1h of
# tools/battery/gpu_shard_files.txt, which is the rule shard 1f states.
tests/test_mynn_pbl.py
tests/test_mp28_runnable.py
tests/test_real_init.py
tests/test_config_freeze.py

# The P3 (mp_physics=50) CPU legs.  On NO battery list before this: the
# CUDA port (commit 2258c85e3) added tests/test_p3_cuda.py and the restart
# work added tests/test_p3_restart_manifest.py, and neither stage 1 nor the
# GPU shard named either, so the whole scheme's gates ran nowhere.
#
# WHAT THAT COST, measured: the port's own scheme-scoping test and its
# table-refusal tests were green only when somebody ran them by hand, and
# the restart defect that killed EVERY mp=50 forecast
# (state._p3_workspace unclassified) was found by a separate lane rather
# than by the battery, because the battery was not asking.
#
# GPU-bound by measurement (tests/conftest.py::_cupy_scope), so that the
# split matches the shard rule rather than the filename: both files below
# report whole=False, functions=empty -- they open no device -- while
# their _gpu.py twins report GPU-bound and go to gpu_shard_files.txt.
tests/test_p3_cuda.py
tests/test_p3_restart_manifest.py

# ---------------------------------------------------------------------------
# The 2026-08-29 verification audit's gates.  Each names the fault injection it
# was written against, and each was proved failing before it was allowed to
# pass; the injections and their measured before/after are in the commit.
# ---------------------------------------------------------------------------

# Every CUDA translation unit pinned by SHA-256, ONE NODE PER MODULE.  The
# fault: `pracw = 0.0f * fminf(...)` in gpuwm/core/kernels/wsm6.cu -- WSM6 rain
# accretion of cloud water switched off -- and the entire stage-1 leg run twice,
# pristine and injected: zero files differed, zero node ids differed.
# tests/test_mp8_frozen.py holds the digests, is red, is deliberately off this
# list because it is red, and even run directly fails with the IDENTICAL two
# node ids clean and injected, because it asserts once over a dict.  This file
# is the same claim per module: under the same fault it fails exactly
# `...[wsm6]`, and a tenth red module is a new node id rather than a longer
# message.  91 modules, 1.07 s, no compile and no device.
tests/test_kernel_source_freeze_per_module.py

# The nine hand-written copies of glibc's exp2f/logf/powf tables.  Row 1 of
# __exp2f_data is transcribed by hand in eight .cu files and once in Python.
# One ULP in noahmp_soilwater.cu alone is caught today only because
# noahmp_water.cu carries a verbatim copy of the same section -- a duplication
# artefact.  The same ULP in BOTH (so the pair agrees) and the same ULP in
# noahmp_snow.cu were caught by nothing at all.  Cross-copy equality catches one
# drifted transcription; the Python anchor catches all eight drifting together.
# 37 nodes, 0.44 s, text only.
tests/test_cuda_libm_table_copies.py

# The private-material scan, tested in the direction where it goes quiet.
# Pointing CLEAN_BASE at a SHA this clone does not carry takes three
# pytest.skips -- including the commit-message scan the module's own docstring
# calls a release blocker -- and the file still exits 0 (measured: 2 passed, 3
# skipped, rc=0).  Narrowing the Tier-1 literal set to its first entry leaves 5
# passed, rc=0, because the module's own falsifiability control exercises one
# token.  This file drives every literal and every pattern, one node each.
tests/test_excluded_subsystem_scan_is_armed.py

# The battery lists' census.  Deleting one entry from this file stops a whole
# suite running and nothing says so: measured at b47a400a5 for
# tests/test_water_overlay.py, tests/test_doctor.py and tests/test_speedrun.py
# one at a time, tests/test_stage1_manifest.py went 362 -> 360 passed and stayed
# GREEN in all three, and test_battery_route.py, test_fastfix_selector.py and
# test_ci_test_replay.py were green too.  The manifest gate pins four entries by
# name and counts nothing.  This one is a bijection over every entry, both ways.
tests/test_battery_list_census.py

# The selector's cost model.  fastfix --record spawns one pytest per file and
# the battery runs one invocation for all of them, so every recorded row carried
# ~2.45 s of interpreter start the battery never pays -- 421 s across this leg,
# and 50-100% of the recorded cost of the 100 files here that are under 5 s,
# which is the end of the range where cheapest-first ordering is decided.
# --record-from reads a batched --durations=0 report instead; this gates the
# parser, including the three ways it can silently measure the wrong thing.
tests/test_fastfix_batched_durations.py

# The writers that land on a TRACKED file. Twenty-one call sites in ten
# files write a path this repository tracks, and `Path.write_text` is
# text mode: one read_text/write_text round trip over
# gpuwm/core/noahmp_water.py takes it from 0 CR bytes to 352. Five of
# the ten destinations had already been flipped whole before this
# landed. It reads ten modules with ast and round-trips their
# destinations through tmp_path -- no import, no device, 1.4 s.
tests/test_tracked_file_writers_keep_lf.py

# The 2.6.0 P3 reachability landing: ten sibling fixes merged after their
# verifying pass was killed, re-reviewed one by one. Their gates go on
# the leg that runs at every cut, because a gate on no list is asked by
# nobody -- the exact class SHARD 1i was written about.
tests/test_p3_legacy_rrtmg_radii.py
tests/test_p3_legacy_cloud_fraction.py
tests/test_n5s_toolchain.py
tests/test_rw_wps_stock_inventory_contract.py
tests/test_acoustic_npref.py

# The P3 mixed-edge named refusal (2.6.0) lives beside the WDM6 and
# Thompson-aerosol ones in this module, and no leg ran it at all.
tests/test_microphysics_transition.py
