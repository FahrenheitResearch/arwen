# The battery's ALWAYS list: the gates no selector can find.
#
# WHAT THIS IS
#   The set of test files that run in EVERY lane, unconditionally, however
#   small the change.  They are the repo-scanning gates and the release
#   governance suites: files that read the tree rather than import it.
#
# WHY IT HAS TO EXIST
#   tools/battery/fastfix.py selects tests by DIRECT import edges from the
#   product files a change touched.  That works -- the 2026-08-13 audit
#   validated it against a real defect -- but it is structurally blind to
#   this class, because a citation checker, a line-ending gate or a receipt
#   regenerator has no import edge to anything.  The same audit measured it:
#   commit 6e9c690f0 left tools/ftz_receipt/receipt/route_inventory.json
#   stale, and tests/test_ftz_route_inventory.py is a MISS at import depth
#   0, 1 AND 2.  A selector that skipped it would have shipped exactly the
#   defect the receipt exists to catch.
#
#   So these are never selected.  They are always run.
#
# WHO CONSUMES IT
#   tools/battery/fastfix.py adds every entry here to every selection, and
#   labels the reason "always (repo-scanning gate)".  tools/battery/FASTFIX.md
#   describes the lane.
#
# FORMAT
#   One repository-relative path per line, forward slashes.  Blank lines and
#   lines whose first non-space character is `#` are ignored -- the same
#   parse as tools/battery/stage1_files.txt, and for the same reason: the
#   audit's own membership check reported a path as listed when it occurred
#   only inside a comment.
#
# AMENDMENT DISCIPLINE
#   As stage1_files.txt: an addition carries its REASON inline, on the lines
#   above it, naming the task or the incident that earned it.  A removal is
#   an argument, not a tidy-up.  This header used to name
#   `tests/test_always_manifest.py` as the gate.  Measured 2026-08-29: NO SUCH
#   FILE HAS EVER EXISTED in this tree, and the only file that reads this list
#   is tests/test_fastfix_selector.py -- so the amendment discipline above was
#   enforced by nothing at all, and an entry could be deleted from here without
#   any gate noticing.  tests/test_battery_list_census.py is the gate now: it
#   holds a recorded test count per entry in tools/battery/list_census.json and
#   checks the bijection both ways, so a deleted entry fails by name and says
#   how much coverage left with it.

# The manifest gates.  Each one is the only thing that stops a tidy-up
# dropping an entry from the list it gates -- including, since the audit
# found the gate on the stage-1 list was not on the stage-1 list, this one's
# own siblings.
tests/test_stage1_manifest.py
tests/test_cargo_gate_manifest.py
tests/test_tiles_gate_manifest.py

# The fourth manifest gate, and the one that counts.  Its three siblings above
# check that a listed path exists, reject duplicates and pin four entries by
# name; none of them notices an entry being DELETED, which was measured on
# three separate entries at b47a400a5 and caught by nothing.  This one holds a
# recorded test count per entry and checks the bijection in both directions, so
# a removal fails by name and says how much coverage went with it.  It reads
# two files and imports nothing, which is what puts it on this list rather than
# only on the cut leg.
#
# It also covers this file: tools/battery/always_files.txt's own header names
# `tests/test_always_manifest.py` as its gate and NO SUCH FILE EXISTS in this
# tree -- measured 2026-08-29 -- so until now the ALWAYS list was the one
# battery list with no manifest gate at all.
tests/test_battery_list_census.py

# Byte-level repository hygiene.  `.gitattributes` sets `* -text`, so an
# LF-to-CRLF rewrite is a real byte change in files the product hashes for
# identity.  Task #168 rewrote thirteen files that way and the stage-1
# battery stayed green through two full runs.
tests/test_line_ending_stability.py

# Release governance over prose and configs.  These read the newest
# CHANGELOG section, the shipped configs and the public claims; no import
# edge reaches any of them, and every lane writes into the section the first
# one checks.
tests/test_release_notes_are_public_facing.py
tests/test_case_token_leakage.py
tests/test_public_claim_backing.py
tests/test_parameter_claim_gate.py
tests/test_physics_md_reachability_claims.py
tests/test_shipped_acknowledgement_justifications.py
tests/test_evidence_axes.py

# The FTZ receipt family: the receipt, the census it must agree with, and
# the prose derived from it.  route_inventory.json pins a file:line and the
# exact source text for every cupy kernel-construction site, and only
# test_ftz_route_inventory.py regenerates it.  This is the family the audit
# proved a depth-0, depth-1 AND depth-2 selector all miss on 6e9c690f0 --
# precisely: the gate imports its own GENERATOR (tools/ftz_receipt/), so
# touching that selects it, but it has no edge to the gpuwm/core/ kernel
# sites it inventories, and those are the edits that make it go stale.
# tests/test_fastfix_selector.py pins that distinction rather than the
# looser sentence, because the looser one is not quite true.
tests/test_ftz_route_inventory.py
tests/test_ftz_claim_consistency.py
tests/test_ftz_render.py

# Provenance and certification hygiene: the seam that refuses every front
# door when the executing tree disagrees with the installed metadata, and
# the capsule hygiene scan.  Cheap, and a regression in either direction is
# a release-stopper.
tests/test_provenance_gate.py
tests/test_provenance_wire.py
tests/test_certify_hygiene.py

# The release machinery's own verifier, earned by the 2.1.0 assembly: seven
# of its cases were red on the assembled branch and the verifier would have
# refused the bundle in the prepare job, after the tag, mid cut.  A fast-fix
# lane publishes; it must never publish through this one un-run.
tests/test_verify_release_artifacts.py

# The gate on the gate-placement class itself (2026-08-13 audit, G1).  It
# AST-walks every test module in the tree, so it has no import edge to any
# product file by construction -- and what it protects is a stage-1 entry
# silently collecting zero tests, which is the exact failure a fast lane
# would inherit and amplify.
tests/test_module_skip_placement.py

# The standalone RW-WPS wheel's staging boundary, promoted here after it
# burned a SECOND release number (2026-08-14).  It was already a stage-1
# entry, and stage 1 is the wrong depth for it twice over.
#
#   It is blind by construction.  The suite STAGES the wheel by path and
#   AST-walks what it staged; it imports gpuwm.native_wrf_distribution and
#   tools.build_rw_wps_release and nothing else of the product.  So it has
#   no import edge to gpuwm/core/streaming.py -- the file whose imports it
#   actually constrains -- and the selector cannot reach it from the edit
#   that breaks it.  Measured on the 2.3.0 range (v2.2.1..81edc6afa): 87
#   product files touched, streaming.py and nest_stream.py among them, 289
#   tests selected, this suite NOT one of them.  That is the same shape as
#   the receipt-inventory gates above and it belongs in the same list.
#
#   It fails only at a cut, which is the most expensive place to learn.
#   A lane adds a function-local import to a staged module; nothing in the
#   tree objects, because the import is correct everywhere except inside a
#   wheel that omits its target.  Twice now the first objection has come
#   from the publish pipeline with the tag already spent: two imports on
#   the 1.8.8 line (the release became 1.8.9) and four on 2.3.0 (this one).
#   Stage 1 covered both and stage 1 does not run in a lane.
#
# Roughly 18 s, the most expensive entry here.  That is the price of the
# gate being run by the lane that breaks it rather than by the cut that
# cannot afford to.
tests/test_native_wrf_distribution.py

# The wheel-pin gate: proves setup.py refuses bdist_wheel while
# gpuwm/data/bridges/bridge-pins.json declares no release and no
# platforms.  Repo-scanning by nature (it reads the tree and runs the
# real setup.py in subprocesses; no import edge reaches it), and the
# defect it pins fails only at a cut: the 2.5.0 candidate wheel was
# built with the pin step skipped and installed on a clean home as
# FAILED bridges -- no GRIB decoder, no NetCDF decoder, renderer
# rw_wrfbatch not built.  About 4 s.
tests/test_wheel_pin_gate.py

# The MYNN sibling-kernel strip proof.  The rule applied is this list's
# own: a file belongs here when fastfix.py can NEVER select it, because it
# has no import edge to any product module -- it reads the tree.  This one
# imports only pathlib/hashlib and then walks gpuwm/core/kernels/*.cu by
# path.  A .cu file is not a Python module, so an edit to the very source
# it gates creates no import edge and the selector is blind to it at every
# depth -- the same shape as the route_inventory.json miss this list was
# created for.
#
# What it proves: mynn_dmp_sibling.cu is the frozen mynn_pbl.cu plus
# complete lines carrying one marker, and stripping every marked line
# restores the frozen bytes exactly.  A sibling that acquires arithmetic
# of its own stops being a rename and becomes a second physics
# implementation under one name.  4 passed, 0.37 s.
tests/test_mynn_dmp_sibling.py

# The exclusion gate.  Same rule, same reason it cannot be selected: it
# imports re/subprocess/pathlib and then walks `git ls-files`, so no edit
# to any file it scans creates an import edge to it.  It is also the one
# gate whose failure is not a bug but a disclosure -- it fails when this
# branch has regained the excluded subsystem it was built to exclude, and
# this branch is what a public 2.5.8 release is cut from.  A gate like
# that must run on every lane, not on the lanes that happen to touch the
# right module.  3 passed, 15.4 s (it reads about 5600 tracked files).
tests/test_excluded_subsystem_absent.py

# ADDED 2026-08-29, and the reason is the defect they exist for.
# fastfix's selector answers a non-Python change (a .cu, a .rs, a receipt)
# with the ALWAYS list AND NOTHING ELSE -- its own docstring says so, because
# import analysis has nothing to say about those files.  These four gates
# READ files rather than importing modules, so the selector can never reach
# them by edge, and four of the five gates built on 2026-08-29 to catch a
# silent CUDA edit were on stage 1 only.  A WSM6 kernel could therefore be
# switched off and the FAST path would still select nothing that looks at it
# -- which is the exact fault they were written for, surviving in the lane
# most likely to be used while iterating.  Measured cost of all four: 2.02 s.
tests/test_kernel_source_freeze_per_module.py
tests/test_cuda_libm_table_copies.py
tests/test_excluded_subsystem_scan_is_armed.py
tests/test_fastfix_batched_durations.py

# The writers that land on a TRACKED file. Twenty-one call sites in ten
# files write a path this repository tracks, and `Path.write_text` is
# text mode: one read_text/write_text round trip over
# gpuwm/core/noahmp_water.py takes it from 0 CR bytes to 352. Five of
# the ten destinations had already been flipped whole before this
# landed. It reads ten modules with ast and round-trips their
# destinations through tmp_path -- no import, no device, 1.4 s.
tests/test_tracked_file_writers_keep_lf.py

# The mutation gate's own manifest and DEBT_CEILING ratchet. Its direct
# siblings test_cargo_gate_manifest.py and test_tiles_gate_manifest.py
# are here, and fastfix cannot select it by import edge (it loads the
# runner via spec_from_file_location) -- a ratchet no leg runs cannot
# refuse a silently grown debt list.
tests/test_mutation_gate_manifest.py
